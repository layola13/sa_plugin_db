# sa_plugin_db Tasks

Living backlog for the remaining SQLite-parity and performance work.

## Current State

- The current configured-run proof evidence is `zig build bench-compare-proof -Dlock-wait-seconds=900` on 2026-07-07. Disk indexed ERP passed all hard stages and reported total append chain db `3.726 ms [3.485, 3.887]` vs SQLite `3.934 ms [3.539, 4.185]` with only `0.207 ms` median margin. Memory indexed ERP total append chain passed at db `2.313 ms [2.195, 2.903]` vs SQLite `3.325 ms [3.017, 4.625]`. Concurrent strict insert stayed correct and faster over 7 runs: raw db `5.986 ms [5.599, 6.981]` vs SQLite `80.419 ms [68.939, 100.006]` with `db_ok=7/7`, and coltx db `7.979 ms [6.896, 10.895]` vs SQLite `80.419 ms [68.939, 100.006]` with `db_ok=7/7`. The remaining work below is mostly about widening the durable disk write margin, reducing variance, and finishing broader SQL, durability, and operational coverage.
- A later accidental `zig build sqlite-proof` run failed the memory indexed ERP init hard gate by a very small margin: db `0.544 ms` vs SQLite `0.534 ms`. Protected benchmark artifacts were clean afterward. This remains negative historical proof-stability evidence; do not claim complete SQLite parity until repeated current proof runs cover both memory and durable disk paths with the existing caveats.
- Memory-root schema bootstrap now defers writing the `.sadb-schema` file into the in-memory filesystem until a file-shaped operation needs it (`verify`, `snapshot`, `recover`, `loadSchema`, or schema hashing). The direct regression proves `:memory:` init and the first row write keep the schema file absent, then `verifyTable` materializes it. The latest `zig build bench-compare-memory -Dbench-compare-runs=7 -Dlock-wait-seconds=900` passed after this change: init db `0.226 ms [0.198, 0.277]` vs SQLite `0.472 ms [0.421, 0.527]`, baseline db `2.657 ms [2.379, 3.163]` vs SQLite `57.643 ms [54.816, 81.741]`, build db `6.542 ms [6.394, 8.316]` vs SQLite `19.120 ms [15.947, 22.734]`, tx append db `1.313 ms [1.258, 1.744]` vs SQLite `3.287 ms [2.984, 3.941]`, coltx append db `0.888 ms [0.812, 1.126]` vs SQLite `3.287 ms [2.984, 3.941]`, verify db `0.190 ms [0.178, 0.279]` vs SQLite `33.012 ms [30.594, 34.134]`, and total append chain db `2.227 ms [2.111, 2.870]` vs SQLite `3.287 ms [2.984, 3.941]`. This is improved memory indexed ERP proof only; disk combined append remains report-only and full SQLite parity remains unproven.
- Unsafe/no-sync memory-root write paths now also preserve deferred schema materialization: `writeCompatMeta` skips `ensureDeferredSchemaMaterialized` for memory roots while still materializing disk no-sync schemas, so direct writes, write transactions, and coltx commits keep the memory schema file absent until a file-shaped reader such as `verifyTable` needs it. The hot-I/O guard now requires this `!isMemoryRoot(root_dir)` boundary and unsafe memory-root direct/tx/coltx regression coverage. The latest `zig build bench-compare-memory -Dbench-compare-runs=3 -Dlock-wait-seconds=900` passed with init db `0.286 ms [0.223, 0.395]` vs SQLite `0.524 ms [0.429, 0.606]`, baseline db `3.000 ms [2.315, 3.078]` vs SQLite `59.330 ms [57.360, 66.407]`, build db `7.252 ms [6.326, 8.664]` vs SQLite `19.590 ms [18.585, 19.783]`, tx append db `1.269 ms [1.252, 1.525]` vs SQLite `3.639 ms [3.237, 3.881]`, coltx append db `0.958 ms [0.857, 1.091]` vs SQLite `3.639 ms [3.237, 3.881]`, verify db `0.231 ms [0.198, 0.300]` vs SQLite `36.452 ms [32.968, 47.203]`, and total append chain db `2.227 ms [2.109, 2.616]` vs SQLite `3.639 ms [3.237, 3.881]`. This removes write-path schema I/O from the no-sync memory benchmark path only; verification, snapshot, recovery, and explicit schema readers still materialize schema as required.
- Direct blob append now skips blob-index rebuilds when the appended blob id is not referenced by any existing row. `putBlobValue` scans mapped blob-handle segment columns for indexes on the target store and only rebuilds if a pre-existing row already points at the newly appended id; appending an unreferenced blob keeps existing `blob_eq`/token/prefix/contains index artifacts unchanged, while later row inserts that reference the new blob still use append-index incremental maintenance. The latest `zig build bench-compare-disk -Dbench-compare-runs=3 -Dlock-wait-seconds=900` passed all hard disk indexed ERP stages after this change: init db `0.180 ms [0.169, 0.214]` vs SQLite `0.823 ms [0.745, 0.929]`, baseline db `4.902 ms [4.630, 5.018]` vs SQLite `66.791 ms [62.745, 70.757]`, build db `9.017 ms [8.587, 9.507]` vs SQLite `20.693 ms [20.502, 21.832]`, tx append db `2.416 ms [2.362, 2.638]` vs SQLite `4.220 ms [3.895, 6.441]`, coltx append db `1.653 ms [1.596, 1.853]` vs SQLite `4.220 ms [3.895, 6.441]`, and verify db `0.953 ms [0.930, 0.985]` vs SQLite `33.774 ms [32.509, 36.146]`. Total append chain remained report-only WARN at db `4.269 ms [3.958, 4.291]` vs SQLite `4.220 ms [3.895, 6.441]`, so this removes one hidden direct blob append rebuild path without changing the future-id correctness case, graduating strict-chain, or claiming full SQLite parity.
- Dictionary/blob artifact writes now avoid redundant parent-directory fsyncs before the later meta/manifest publication makes them durable. `flushPendingDictWrites`, `flushPendingBlobWrites`, unsafe cached dict/blob materialization in `writeCompatMeta`, direct `internStringDict`, direct `internStringDictMany`, and direct `putBlobValue` now all use `writeArtifactFile`, which still writes and syncs file contents as configured but skips per-artifact parent-directory sync. The latest `zig build bench-compare-disk -Dbench-compare-runs=3 -Dlock-wait-seconds=900` passed all hard disk indexed ERP stages: init db `0.197 ms [0.176, 0.255]` vs SQLite `0.986 ms [0.742, 0.993]`, baseline db `4.697 ms [4.445, 4.883]` vs SQLite `78.009 ms [68.986, 82.123]`, build db `9.241 ms [9.136, 9.591]` vs SQLite `20.688 ms [18.191, 26.694]`, tx append db `2.342 ms [2.257, 2.434]` vs SQLite `4.426 ms [3.947, 6.190]`, coltx append db `1.531 ms [1.518, 1.684]` vs SQLite `4.426 ms [3.947, 6.190]`, verify db `1.034 ms [0.919, 1.081]` vs SQLite `45.456 ms [34.862, 50.361]`, and total append chain db `3.860 ms [3.788, 4.118]` vs SQLite `4.426 ms [3.947, 6.190]`. This is only a 3-run durable-path sample and only removes redundant artifact directory syncs; at that point blob/dictionary append still built replacement artifact buffers, disk strict-chain remained opt-in, and this was not a full SQLite parity claim.
- Batched string dictionary intern now scans an existing dictionary once per batch instead of repeatedly calling `dictFindValueId(old_bytes, value)` for each input. `dictScanCountAndFindValueIds` validates the dictionary while filling existing ids for all requested values, and both the durable `internStringDictMany` path and unsafe bootstrap cache path then only handle pending new values and duplicate values within the current batch. The latest `zig build bench-compare-disk -Dbench-compare-runs=3 -Dlock-wait-seconds=900` passed hard disk indexed ERP stages after this change: init db `0.186 ms [0.179, 0.332]` vs SQLite `0.811 ms [0.728, 5.044]`, baseline db `4.533 ms [4.280, 4.737]` vs SQLite `65.304 ms [62.307, 73.728]`, build db `9.512 ms [8.221, 10.523]` vs SQLite `21.732 ms [17.975, 25.688]`, tx append db `2.801 ms [2.404, 3.856]` vs SQLite `4.160 ms [3.788, 5.839]`, coltx append db `1.757 ms [1.643, 2.006]` vs SQLite `4.160 ms [3.788, 5.839]`, and verify db `1.004 ms [0.823, 1.211]` vs SQLite `35.089 ms [33.970, 38.055]`. Total append chain remained report-only WARN at db `4.807 ms [4.047, 5.613]` vs SQLite `4.160 ms [3.788, 5.839]`, so this removed repeated old-dictionary scans in batched intern only; at that point dictionary append still built replacement artifact buffers, disk strict-chain remained opt-in, and this was not a full SQLite parity claim.
- Direct durable dictionary/blob append now streams counted-artifact metadata and writes instead of constructing a full replacement artifact buffer. `makeCountedArtifactHashes` computes SHA-256 and block hashes over `new-count + old_payload + appended entries`, and `writeCountedArtifactFile` writes the same layout through a temp artifact before meta publication. Durable direct `internStringDict`, `internStringDictMany`, and `putBlobValue` use these helpers; transaction pending writes, unsafe bootstrap cache writes, and memory-root fallback writes still keep full artifact bytes because they must serve uncommitted or cached read views. The latest `zig build bench-compare-disk -Dbench-compare-runs=3 -Dlock-wait-seconds=900` passed hard disk indexed ERP stages after this change: init db `0.177 ms [0.167, 0.313]` vs SQLite `1.122 ms [0.819, 4.734]`, baseline db `4.187 ms [4.132, 4.490]` vs SQLite `66.022 ms [58.541, 74.628]`, build db `8.532 ms [8.332, 9.632]` vs SQLite `19.089 ms [18.914, 29.781]`, tx append db `2.293 ms [2.250, 2.327]` vs SQLite `4.476 ms [3.615, 6.353]`, coltx append db `1.613 ms [1.596, 1.627]` vs SQLite `4.476 ms [3.615, 6.353]`, verify db `0.840 ms [0.833, 0.984]` vs SQLite `36.702 ms [31.872, 37.362]`, and total append chain db `3.889 ms [3.863, 3.954]` vs SQLite `4.476 ms [3.615, 6.353]`. This is still a 3-run durable-path sample; disk strict-chain remains opt-in and this is not a full SQLite parity claim.
- Direct durable dictionary/blob append now also combines counted-artifact file writing and SHA/block-hash generation into one streaming pass. `writeCountedArtifactFileAndHashes` writes the temp artifact while hashing the same `new-count + old_payload + appended entries` stream, and direct `internStringDict`, `internStringDictMany`, and `putBlobValue` build meta from the returned hashes via `makeDictMetaFromCountedArtifactWrite` / `makeBlobStoreMetaFromCountedArtifactWrite`. This removes the separate durable disk old-payload scan after the previous replacement-buffer removal; transaction pending writes, unsafe bootstrap cache writes, and memory-root/no-sync fallbacks still keep their existing full-byte or no-sync semantics. The latest `zig build bench-compare-disk -Dbench-compare-runs=3 -Dlock-wait-seconds=900` passed hard disk indexed ERP stages with init db `0.182 ms [0.169, 0.207]` vs SQLite `0.747 ms [0.745, 0.869]`, baseline db `4.372 ms [4.329, 4.533]` vs SQLite `60.264 ms [54.203, 64.904]`, build db `8.506 ms [8.259, 9.409]` vs SQLite `19.158 ms [18.928, 19.449]`, tx append db `2.248 ms [2.188, 2.649]` vs SQLite `3.729 ms [3.671, 3.969]`, coltx append db `1.657 ms [1.639, 1.697]` vs SQLite `3.729 ms [3.671, 3.969]`, and verify db `0.903 ms [0.855, 0.911]` vs SQLite `32.762 ms [32.265, 34.783]`. Total append chain remained report-only WARN at db `3.887 ms [3.846, 4.347]` vs SQLite `3.729 ms [3.671, 3.969]`, so disk strict-chain remains opt-in and this is not a full SQLite parity claim.
- Transaction and unsafe-bootstrap dictionary/blob staging now build pending counted-artifact bytes and SHA/block-hash metadata in one pass. `buildCountedArtifactBytesAndHashes` fills the required in-memory artifact while updating the same streaming file/block hash state, and `writeTransactionInternStringDict`, `writeTransactionPutBlobValue`, direct unsafe-bootstrap dictionary/blob branches, and unsafe cached batched dictionary intern build meta from those hashes via `makeDictMetaFromCountedArtifactBuild` / `makeBlobStoreMetaFromCountedArtifactBuild`. These paths still intentionally keep full artifact bytes because transaction read views, rollback cleanup, commit flush, and unsafe cached materialization need them; this removes a redundant staged-buffer hash scan only and is not a new benchmark proof or full SQLite parity claim.
- Memory-root direct counted-artifact writes now also use the staged byte+hash helper. The `mem://` branch of `writeCountedArtifactFileAndHashes` builds the replacement artifact bytes and hash metadata together before copying the bytes into the in-memory filesystem, instead of calling `makeFileHashesSinglePass` over the completed buffer. This only removes a memory-root fallback duplicate hash scan; durable disk, transaction, and unsafe cache ownership semantics remain unchanged, and this is not new SQLite parity proof.
- Full-buffer segment/raw/staged column, compact merge, epoch column rewrite, and index rewrite/rebuild artifact writes now build metadata hashes through `writeFileWithParentSyncAndHashes` instead of writing first and then calling `makeFileHashesSinglePass` for metadata. This preserves the existing temp-write/rename and optional parent-sync semantics, but these paths still allocate or assemble the full output buffer before writing; it is not a benchmark proof or full SQLite parity claim.
- Unsafe/no-sync in-place index merge now validates fixed-width single-column indexes, variable blob-text indexes, and pair indexes while writing the merged mmap/memory buffer instead of doing a second full validation scan afterward. The inline checks preserve exact byte-count, row-range, ordering, unique-key, and variable duplicate invariants, and the regression `table unsafe in-place index merge validates while merging` covers valid merges plus corrupt ordering, duplicate variable entries, and row-count byte mismatches. The latest `zig build bench-compare-disk -Dbench-compare-runs=3 -Dlock-wait-seconds=900` still passed hard stages after this work: init db `0.187 ms [0.166, 0.289]` vs SQLite `0.736 ms [0.722, 6.772]`, baseline db `4.411 ms [4.168, 5.385]` vs SQLite `63.749 ms [62.828, 64.861]`, build db `10.839 ms [8.089, 10.845]` vs SQLite `18.038 ms [17.271, 20.979]`, tx append db `2.786 ms [2.243, 3.237]` vs SQLite `3.782 ms [3.637, 4.035]`, coltx append db `2.050 ms [1.446, 2.090]` vs SQLite `3.782 ms [3.637, 4.035]`, and verify db `1.130 ms [0.868, 1.197]` vs SQLite `34.073 ms [32.686, 35.017]`. Total append chain was still report-only WARN at db `4.835 ms [3.689, 5.328]` vs SQLite `3.782 ms [3.637, 4.035]`, so this is hot-path validation work, not evidence to graduate disk combined append to a hard gate or claim full SQLite parity.
- `zig build bench-compare` now acts as the default SQLite-control gate for both disk and memory indexed ERP stages: row-count parity, init, baseline ingest, index build, `tx` append, `coltx` append, and verify. The combined append chain is still reported by default but remains aspirational as a strict gate until the durable disk write margin is wider and less variance-sensitive.
- Benchmark compare runners now validate db/SQLite output metrics explicitly before row-count, correctness, median, or margin interpretation. Non-positive or even run counts fail during build configuration before benchmark install/build steps are wired, runner code still rejects bad run counts defensively before sampling, reported medians are always observed samples, and runner code now sorts timing values and takes the middle observed sample instead of relying on interpolation. Strict-chain and strict-insert flags must be explicit `0/1` values, missing indexed ERP/concurrent metrics fail with sample-specific diagnostics, any non-integer `key=value` metric fails immediately as an invalid integer metric, negative metrics fail immediately, concurrent `*_ok` status metrics must be `0` or `1`, duplicate metric names fail instead of letting later output silently overwrite earlier values, timing `*_ns` metrics must be positive, and concurrent insert row/status metrics must be internally consistent: rows cannot exceed `50000`, `ok=1` must mean the full `50000` rows were inserted, and `ok=0` must not claim the full row count. This improves evidence integrity but does not change the disk combined append report-only policy, concurrent insert strictness opt-in policy, strict-chain opt-in policy, or SQLite parity caveats.
- The compare gate now prints `median [min,max]` and median margin for every stage. Recent 7-run samples show disk combined append can lead SQLite, including the current proof sample above, but the margin remains small and prior default proof samples still produced report-only `WARN` results. Default graduation still needs repeated stable runs with wider margins.
- `zig build bench-compare-proof` is now the preferred configured-run proof/report target (default 7 runs, and configured run counts must be positive odd values): disk and memory indexed ERP comparisons run with strict chain disabled, while concurrent insert is strict. It prints a final pass summary only after the post-run protected artifact guard completes, and that summary includes the actual configured run count as `runs=N`. Disk combined append remains report-only by default despite the latest positive sample; use `bench-compare-proof-strict-chain` or `sqlite-proof-strict-chain` only for intentional upgrade evidence collection.
- The default `zig build bench-compare` now also runs the concurrent comparison gate. Raw and coltx serial/concurrent SUM query paths are each hard gates against SQLite, so one fast path can no longer hide the other path regressing. Concurrent inserts are now correct and fast on both raw and coltx paths; `zig build bench-compare-concurrent -Dbench-compare-strict-concurrent-insert=true` passed with `db_ok=3/3` for both paths. `zig build bench-compare-concurrent-strict-insert` is the explicit narrow opt-in target for repeated strict insert evidence using `-Dbench-compare-proof-runs`; it now prints a final pass summary with `runs=N` only after strict raw/coltx insert proof and the post-run protected artifact guard complete. The default gate still leaves insert strictness opt-in until more repeated runs are collected.
- Installed-plugin smoke now covers the full CLI lifecycle `init -> ingest -> snapshot -> ingest -> compact -> lock -> failed write -> unlock -> ingest -> restore -> recover -> verify`. A restore is now published as a new committed epoch, so a later recover no longer jumps back to a higher pre-restore versioned meta.
- `zig build test` now also runs `abi-smoke`, which builds and executes the broad `benchmark_test/db_*_smoke.sa` public `db.sal` suite against the installed dev plugin from temporary cwd roots. This covers lifecycle, memory roots, typed numeric queries, blob stores/indexes, grouped aggregation, candidate filters, planners, transactions, typed key reads/writes, compact/sort, and date/decimal planner flows through the public ABI rather than only Zig internals.
- `zig build test` now also runs `crash-recover-smoke`, an installed CLI recovery smoke that simulates a stale active manifest, a committed transaction marker with a stale manifest, an incomplete pending transaction marker, corrupt latest segment/index/dictionary artifacts, and a corrupt latest versioned meta. This proves the public `sa db recover` command can republish the highest valid commit, complete marked committed transactions, ignore and clean uncommitted pending epochs, and fall back when the newest meta/artifact candidate is torn/corrupt.
- `zig build test` now also runs `check-abi-symbols`, which parses every `sa_db_*` extern declared in `db.sai` and verifies the installed `libdb.so` exports exactly the same symbol set. Current coverage is `306 / 306`, preventing public ABI drift as the plugin surface expands.
- `zig build test` now also runs `check-sal-facade`, which verifies `db.sal` references every `sa_db_*` extern declared in `db.sai` and no undeclared `sa_db_*` symbol. Current facade coverage is `306 / 306`, preventing SA-facing macro facade drift from the native interface.
- `zig build test` now also runs `check-manifest-layout`, and `install-smoke` reruns the same check after dev install. This locks the plugin identity, `db.sai`/`db.sal` interface paths, Linux artifact path, `database` skill, no-net/no-env/no-process-spawn policy, `$PROJECT/**` fs permissions, installed `current/{libdb.so,sap.json,sa/db.sai,sa/db.sal,permissions.lock}` layout, byte-for-byte equality between installed `sa/db.sai` / `sa/db.sal` and the source interface files, byte-for-byte equality between installed `libdb.so` and the manifest artifact `zig-out/lib/libdb.so`, and installed `permissions.lock` fields including the canonical `sap.json` permissions hash.
- `zig build check-benchmark-artifacts` now verifies protected tracked benchmark artifacts stay clean, and `zig build test` depends on it directly. Public compare targets run this guard before benchmark scripts, and `bench-compare`, `bench-compare-memory`, `bench-compare-concurrent`, `bench-compare-concurrent-strict-insert`, and `bench-compare-proof` run it again after comparison. This prevents SQLite comparison evidence from starting on dirty `.bench_*` roots or silently dirtying tracked SQLite artifacts, and prevents the default test gate from passing on dirty protected benchmark evidence.
- Public compare targets now print final pass summaries only after their post-run protected artifact guards complete. This includes `bench-compare`, `bench-compare-disk`, `bench-compare-memory`, and `bench-compare-concurrent`, and each final line includes the actual configured run count as `runs=N`, so a successful final line means the comparison completed with the displayed sample count and the protected benchmark artifacts were still clean.
- `zig build test` now also runs `check-benchmark-parser-guards`, which verifies the benchmark compare scripts still fail on missing required metrics, invalid-integer metrics, negative metrics, non-boolean concurrent status metrics, duplicate metric names, or non-positive timing metrics before interpreting row counts, medians, margins, or strict-chain results. This is benchmark parser guards evidence only; it does not execute benchmark comparisons and is not a claim of full SQLite parity.
- `zig build test` now also runs `check-benchmark-hot-io-guards`, which prevents benchmark-relevant hot I/O paths from regressing to broad `readFileAlloc(..., 1 << 30)` heap-copy reads, prevents the old owned dictionary/blob helpers from returning, and locks the mapped input wiring for bounded copy/ingest inputs, append-index column cache, compact/full-buffer rebuild inputs, direct `u64` range-update old inputs, direct/transaction changed-column index rebuild wiring, and direct/transaction no-op row replacement short circuits. This is hot-I/O wiring evidence only; it does not execute benchmark comparisons and is not a claim of full SQLite parity.
- CSV/JSONL `ingestTable` now uses the same append-index incremental maintenance path as raw column ingest after publishing a new segment, and only falls back to `rebuildIndexes` when an index kind cannot be maintained incrementally. This removes a hidden full-index rebuild from indexed CLI ingest append paths, while ingest still parses one whole mapped source and is not a claim of full SQLite parity.
- Direct `u64` range updates now rebuild only indexes that reference the updated column, including pair indexes whose second column is updated, instead of rebuilding every table index after rewriting the touched column files. This reduces hidden index write amplification for update paths, while rewrite-style updates still rebuild affected index artifacts and are not a claim of SQLite-level in-place update behavior.
- Direct `u64` range updates now also short-circuit empty ranges and `delta == 0` after validating table lock, column type, and range bounds. These no-op calls return `0` without publishing a new epoch, segment file, or index artifact, while real non-zero updates still rewrite touched column files and rebuild affected indexes.
- Raw-row replacement now records which columns actually change and rebuilds only indexes that reference those changed columns. Updating a non-indexed column can keep existing single-column and pair index artifacts untouched, while updating an indexed column still rebuilds the affected index artifacts. The data files are still republished through the current rewrite-style single-segment model, so this reduces index write amplification without claiming SQLite-style in-place row updates.
- Write-transaction row replacement now carries the same changed-column index rebuild policy through commit when no row-count-changing or append-materializing operation has forced a full rebuild. Transaction updates that only change non-indexed columns can keep existing index artifacts untouched, while updates to indexed columns rebuild only affected single-column or pair indexes. Transaction deletes, materialized pending appends, and other row-count-changing materialized-buffer writes still force full index rebuilds, and data files still rewrite through the current transaction rewrite path.
- Direct and write-transaction row replacement now short-circuit fully no-op updates. If the replacement row bytes are identical to the committed/current transaction row bytes and no earlier transaction mutation needs committing, the update returns the current table info without publishing a new epoch, segment, or index artifact. This reduces avoidable no-op write amplification only; real changed rows still rewrite data files, affected indexes still rebuild whole artifacts, and transaction deletes/row-count-changing materialized writes still full-rebuild indexes.
- Direct delete-by-key and pure write-transaction delete now update existing index artifacts by validating the old index bytes, filtering deleted rows, and decrementing row ids after deleted rows for fixed-width single-column indexes, pair indexes, `blob_eq`, and blob text indexes. Multi-row transaction delete index rewrites use sorted deleted-row lower-bound lookup instead of scanning the whole deleted-row list for every index entry, and regression coverage now verifies `blob_eq`, `blob_token`, `blob_prefix`, and `blob_contains` rows are filtered and migrated correctly after pure transaction deletes. This avoids a full table-column/blob rescan for supported delete-only index maintenance, but the table data files are still rewritten through the current single-segment model and mixed transactions still fall back to full index rebuilds.
- Clean write-transaction unique-key lookups now use committed unique index artifacts before materializing transaction column buffers when the transaction has no row changes, no pending appends, no buffers, and no deleted rows. This covers typed numeric keys, `(u64, u64)` / `(u64, i64)` pair keys, and unique `blob_eq` keys for transaction update/upsert/delete/get-row helper paths, while non-append-only pending/mixed transactions still fall back to the old materialized-buffer scan semantics.
- Append-only write-transaction unique-key lookups now avoid materializing the committed base: they validate committed unique indexes against the transaction base row count, return committed hits directly, and scan only `pending_append_buffers` for committed misses. This covers typed numeric keys, `(u64, u64)` / `(u64, i64)` pair keys, unique `blob_eq` keys including transaction-local blob-store writes, and the new-key upsert path, while committed-row replacement/delete/mixed/materialized-buffer transactions stay on the conservative materialized-buffer path.
- Pending append row replacement now keeps append-only write transactions append-only. When update/upsert targets a row at or after `base_row_count`, `txReplaceRawRow` updates only `pending_append_buffers` before any committed-base materialization, so same-transaction `append -> update` and `append/upsert new key -> upsert same pending key` can still commit through incremental append index maintenance instead of forcing base materialization or changed-column/full-rebuild state.
- Pending append row deletion now keeps append-only write transactions append-only when the deleted row has not reached committed storage. `txDeleteRow` removes row slices directly from `pending_append_buffers` before committed-base materialization, shifts later pending rows down, avoids committed-delete tracking/full-index rebuild state, and turns `append -> delete all pending rows` into a no-row-change no-op unless blob/dictionary metadata was also dirtied.
- Write-transaction blob puts now stage blob artifact bytes in `pending_blob_writes` and flush them during commit before row append-index maintenance needs the new blob store. Rollback frees the pending bytes without creating an uncommitted blob artifact, while same-transaction `blob_eq` lookup/update/upsert validation reads the transaction blob-store view.
- `zig build test` now also runs `check-abi-smoke-coverage`, which requires every `benchmark_test/db_*_smoke.sa` file to be listed in `build.zig`'s `abi_smoke_sources`. Current coverage is `43 / 43`, preventing new public ABI smokes from being added without entering the default gate.
- `zig build test` now also runs `check-docs`, which dynamically parses docs-relevant targets from `build.zig` (`sqlite-*`, `bench-compare*`, `check-docs`, `check-proof-wiring`, and `check-benchmark*`) and requires `tasks.md` / `progress.md` to mention them, plus the disk combined append report-only / not-a-full-parity caveats. The docs guard also checks that the `sqlite-audit` pass summary explicitly mentions the docs guard and benchmark parser guards, that `benchmark_test/RESULTS.md` describes proof targets as configured-run/default-7 with `runs=N` output rather than fixed 7-run only, and that benchmark results use raw/coltx per-path query gates instead of best-path query summaries.
- `zig build test` now also runs `check-proof-wiring`, which locks the `sqlite-audit`, `sqlite-proof`, and `sqlite-proof-strict-chain` proof wiring so broad readiness/proof entry points continue to depend on benchmark executable builds, protected artifact guards, `sqlite-audit`, and the matching proof artifact chains. This is wiring evidence only; it does not execute benchmark comparisons by itself and is not a claim of full SQLite parity.
- `install-smoke` now also runs `check_cli_surface.sh`, which parses the `database` skill items from `src/plugin.zig` and compares them exactly with both installed `sa skills` text output and installed `sa skills --json` machine-readable output. Current installed CLI surface coverage is `14 / 14`, preventing command-surface drift after packaging or JSON automation changes.
- `zig build sqlite-audit` now provides a single non-benchmark SQLite-readiness audit target. It runs the default test gate set, docs guard, benchmark parser guards, proof wiring guard, protected benchmark artifact cleanliness, and `zig build bench` so both db and SQLite control benchmark executable builds still compile before proof evidence is trusted; its final summary explicitly lists benchmark executable builds, benchmark parser guards, proof wiring, and protected benchmark artifact cleanliness. Use it for packaging, ABI, CLI, recovery, public-smoke, benchmark-build, and wiring evidence; keep `bench-compare-proof` as the separate configured-run performance proof/report target. `zig build sqlite-proof` combines both targets into the broadest current default proof entry, while still keeping disk combined append chain report-only. `zig build sqlite-proof-strict-chain` is now the opt-in broad proof that also hard-gates disk/memory combined append chain.
- SQLite control benchmark linking rewrites `libsa_std.a` SQLite stub symbols through a unique temporary `sqlite_link_std_work.*` archive directory beside the generated archive. That rewrite path installs a shell cleanup trap immediately after creating the work directory, so failures in `ar`, `objcopy`, or archive repackaging do not leave stale object files for later proof runs and concurrent builds do not share the same object directory; `check-proof-wiring` locks the `mktemp` work directory, cleanup trap, and `sqlite3_prepare` / `sqlite3_step` / `sqlite3_finalize` symbol rewrites.
- Shared install-smoke home access is serialized with `flock` in `build.zig` for dev install, installed layout checks, CLI surface checks, installed smoke, and crash recovery smoke. This prevents two independent `zig build` processes from racing on `.zig-cache/db-install-smoke-home` and producing false packaging/audit failures.
- Shared benchmark compare home access is also serialized with `flock` in `build.zig` for benchmark dev install and SA benchmark compilation. `abi-smoke` build/run steps share the install-smoke lock as well. This prevents concurrent `zig build bench`, `bench-compare-*`, `sqlite-audit`, or `test` invocations from reading half-installed plugin homes.
- Benchmark comparison execution and protected artifact checks are serialized with `.zig-cache/db-benchmark-run.lock`. This prevents concurrent `bench-compare-memory`, `bench-compare-concurrent`, `bench-compare`, and `bench-compare-proof` runs from cleaning each other's shared benchmark roots or letting artifact guards inspect intermediate state. Compare runners now print child benchmark stdout/stderr before reporting a non-zero exit code.
- Shared build locks are now bounded with `flock -w` and the configurable `-Dlock-wait-seconds=N` option, defaulting to `900` seconds. This prevents install-smoke, ABI-smoke, benchmark build, benchmark compare, and protected artifact guards from waiting forever behind a stale or long-running peer process; `check-proof-wiring` locks the proof-side dependency chain and `check-bounded-locks` now provides the bounded lock guard that rejects any `build.zig` `flock` call without `-w lock_wait_seconds_arg`.

## SQLite Parity

- Keep named and exact `:memory:` roots fully backed by the in-memory filesystem for schema, snapshot, and recovery flows.
- Keep `init -> ingest -> snapshot -> restore -> recover -> verify` stable after table reuse, lock/unlock, and compaction; this is now covered by `install-smoke`, but should remain a required regression gate for recovery changes.
- Keep stale-manifest, committed-transaction-marker, incomplete-pending-marker, corrupt-latest-segment, corrupt-latest-index, corrupt-latest-dictionary, and corrupt-latest-meta recovery stable through the public CLI; this is now covered by `crash-recover-smoke` and should remain a required regression gate for manifest/versioned-meta/artifact/transaction-marker changes.
- Audit remaining write and read paths for hidden rebuilds or file rereads that SQLite does not pay on the hot path.
- Keep hot unique-key lookup/write paths free of avoidable heap copies. Typed numeric, pair, and `blob_eq` unique-key row lookup/update/upsert/delete paths now read index artifacts through mapped read regions on disk; clean write-transaction typed/pair/`blob_eq` key lookups reuse committed unique indexes without materializing full transaction column buffers; append-only write-transaction typed/pair/`blob_eq` key lookups validate committed indexes against the base row count and scan only pending append buffers for misses; pending append row update/upsert/delete rewrites only pending append buffers before committed-base materialization; transaction blob puts now stay pending until commit and transaction `blob_eq` reads use that pending blob-store view; row replacement/deletion plus unique `blob_eq` verification now map segment column inputs; direct and pure transaction delete index maintenance validates and rewrites existing index bytes instead of rescanning all table columns, and multi-row transaction delete rewrites use lower-bound deleted-row adjustment for better scaling; selected blob value len/copy, `blob_eq` key verification, transaction `blob_eq` lookup, blob index construction, append-index `CachedBlobStore`, append-index segment-column cache, blob text index column inputs, compact/full-buffer rebuild inputs, `u64` range-update old column inputs, bounded copy/ingest data-source inputs, and blob append old-input reads now map their disk inputs; direct and transaction string-dictionary lookup/intern/value-copy paths now map dictionary inputs too. Continue auditing remaining row materialization and blob/dictionary append replacement-buffer costs.
- Compare planner choices and row-filter selectivity against SQLite controls for the composite, blob, date, decimal, and bool benchmarks.
- Track any missing ABI surfaces or CLI command gaps against `db.sai` and `db.sal`.
- Keep `db.sai` declarations and `libdb.so` exports exactly synchronized; `check-abi-symbols` is now the hard gate for this.
- Keep `db.sal` macro facade coverage synchronized with `db.sai`; `check-sal-facade` is now the hard gate for this.
- Keep `sap.json` and the installed plugin layout synchronized with the shipped library/interfaces and least-privilege policy; `check-manifest-layout` is now the hard gate for this.

## Performance

- Reduce metadata rebuild work on hot ingest, update, and transaction paths.
- Continue replacing disk hot-path full-file heap copies, redundant validation passes, avoidable schema I/O, hidden full rebuilds, and redundant durable-write syncs where validation can still enforce byte count, checksum, and shape invariants; unique-key index lookup paths, clean and append-only transaction key lookup fast paths, append-only pending-row replacement/deletion, transaction blob commit-time pending writes, segment-column inputs for replacement/deletion, selected direct/index-build blob store scans, append-index `CachedBlobStore`, append-index segment-column cache, unsafe/no-sync compat meta deferred-schema preservation, unsafe/no-sync in-place index merge inline validation, blob text index column scans, compact merge inputs, full column-buffer rebuild inputs, `u64` range-update old column inputs plus column-scoped affected-index rebuilds and zero-count/zero-delta no-op short circuits, direct and transaction raw-row replacement changed-column index rebuilds plus no-op update short circuits, direct and pure transaction delete index-row filtering with lower-bound multi-delete row adjustment and blob text coverage, bounded copy/ingest data-source inputs, blob append old inputs plus unreferenced direct blob append index-rebuild skips, string-dictionary read/intern old inputs plus batched old-dictionary single-scan intern, direct durable dictionary/blob counted-artifact append single-pass write/hash streaming, staged transaction/unsafe/memory counted-artifact byte+hash building, full-buffer segment/index write-time hash metadata, dictionary/blob artifact parent-fsync de-dupe before meta/manifest publish, and CSV/JSONL ingest indexed-append maintenance are covered. Transaction pending dictionary/blob writes, unsafe bootstrap cache writes, and memory-root counted-artifact fallbacks still keep full artifact bytes for read-view/cache/memory-fs semantics, but no longer re-hash those bytes in a separate metadata pass; compact/full-buffer paths still assemble and write merged/replacement outputs even though they no longer post-write re-scan those buffers for metadata, ingest still parses one whole mapped source, affected range-update/raw-row indexes still rebuild whole index artifacts, non-append-only dirty transaction key lookups with pending row visibility still materialize buffers, committed-row deletes still rewrite current data files, mixed transaction deletes/row-count-changing materialized writes still full-rebuild indexes, and rewrite-style update/delete data-file costs still need targeted review before claiming SQLite-level I/O behavior.
- Measure `db_member`, `db_concurrent`, `db_coltx_concurrent`, and ERP benchmark deltas against the SQLite controls after each change.
- Keep index maintenance incremental for the supported append paths and fall back to rebuild only when the write shape is genuinely unsupported.
- Remove redundant schema and manifest reads when a table is already hot in memory.
- Keep memory-root schema materialization deferred on the init/write hot path; schema files should only be materialized for verification, snapshot, recovery, or other file-shaped schema readers.
- Widen the durable indexed ERP write lead first, then chase the remaining query/insert variance and durability gaps.
- Collect repeated strict concurrent insert runs for both raw `sa_db_ingest_columns` and `sa_db_coltx_*` paths under isolated benchmark roots; the last two 7-run proof samples reached raw/coltx `db_ok=7/7`, so the remaining work is proving this stability across repeated CI-like runs before graduating `-Dbench-compare-strict-concurrent-insert=true` into the default hard gate.

## Tooling

- Keep `zig build`, `zig build test`, and `zig build bench` green.
- Keep `zig build bench` building both db benchmarks and SQLite control benchmarks, including the local `libsa_std` SQLite-stub rewrite.
- Keep `zig build bench-compare` green for the stable disk and memory SQLite-control stages after changes that touch build wiring, install layout, unsafe/no-sync metadata, transactions, coltx, indexes, memory roots, or benchmark roots.
- Keep benchmark compare metric integrity checks strict; non-positive or even run counts, non-`0/1` strict-chain or strict-insert flags, missing required indexed ERP, SQLite control, or concurrent db/SQLite metrics, any invalid-integer metric, any negative metric, non-boolean concurrent status metrics, duplicate metric names, non-positive timing metrics, or inconsistent concurrent insert `ok`/row-count pairs must fail before medians, margins, row-count parity, insert correctness, or strict-chain conclusions are trusted.
- Keep `zig build bench-compare-disk` available as the narrower disk-only indexed ERP gate for changes that affect durable write/read paths without rerunning memory and concurrent proof stages; treat 1-run failures as variance diagnostics and use the default median run for gate evidence.
- Keep `zig build bench-compare-memory` available as the narrower memory-only gate for changes that only affect `:memory:` behavior.
- Keep `zig build bench-compare-concurrent` green for per-path raw/coltx serial and concurrent query hard gates plus concurrent insert reporting; keep `zig build bench-compare-concurrent-strict-insert` and the strict insert option green after concurrency changes.
- Keep `zig build abi-smoke` green for broad public `db.sal` regressions; add future parity smokes here when behavior must be proven from SA user code rather than Zig internals.
- Keep `zig build crash-recover-smoke` green for public CLI stale-manifest, transaction-marker, corrupt segment/index/dictionary artifact, and corrupt-meta recovery regressions.
- Keep `zig build check-abi-symbols` green so every public `db.sai` extern has a matching exported native symbol and no undeclared `sa_db_*` exports appear.
- Keep `zig build check-sal-facade` green so every public `db.sai` extern remains reachable from the shipped `db.sal` facade and no undeclared `sa_db_*` call is introduced.
- Keep `zig build check-manifest-layout` green and keep the installed-layout check inside `install-smoke`, so packaging regressions, stale installed interface files, stale installed native libraries, and stale installed permission locks are caught before benchmark or ABI evidence is trusted.
- Keep `zig build check-benchmark-artifacts` green as part of `zig build test` and before/after benchmark comparison targets; protected tracked artifacts must remain clean after `bench-compare`, `bench-compare-memory`, `bench-compare-concurrent`, `bench-compare-concurrent-strict-insert`, and `bench-compare-proof`.
- Keep public compare target summaries behind their post-run protected artifact guards; final `bench-compare* passed` lines must not print before artifact cleanliness has been checked.
- Keep `zig build check-benchmark-parser-guards` green as part of `zig build test`; benchmark parser guards must continue to lock non-positive run count, non-`0/1` strict flag, missing required metric, invalid-integer metric, negative metric, non-boolean concurrent status metric, duplicate metric, non-positive timing, per-path concurrent query gates, and concurrent insert row/status consistency failures before any SQLite comparison evidence is trusted.
- Keep `zig build check-benchmark-hot-io-guards` green as part of `zig build test`; hot-I/O guards must continue to reject broad 1GiB heap-copy reads, obsolete owned dictionary/blob helpers, and unmapped benchmark-relevant input paths before any SQLite-level performance evidence is trusted.
- Keep `zig build check-bounded-locks` green as part of `zig build test`; the bounded lock guard must continue to reject any shared `flock` command in `build.zig` that lacks `-w lock_wait_seconds_arg`, so a stale peer process fails explicitly instead of recreating an unbounded build wait.
- Keep `zig build check-abi-smoke-coverage` green; every `benchmark_test/db_*_smoke.sa` must be part of `abi-smoke` before its evidence is trusted.
- Keep `zig build check-docs` green; `tasks.md` and `progress.md` must mention every docs-relevant target dynamically parsed from `build.zig` and preserve the disk combined append report-only, strict-chain, and not a claim of full SQLite parity caveats. Keep `benchmark_test/RESULTS.md` aligned with configured-run proof behavior, default 7-run wording, `runs=N` pass summaries, and raw/coltx per-path query gates; it must not reintroduce best-path query summaries. Keep the `sqlite-audit` summary aligned with the docs guard so audit output reflects every default gate it ran.
- Keep `zig build check-proof-wiring` green; `sqlite-audit` must keep running the default test gate, benchmark executable builds, and protected artifact guard, while `bench-compare-proof` / `bench-compare-proof-strict-chain` must print their final summaries only after the matching proof artifact guard and `sqlite-proof` / `sqlite-proof-strict-chain` must depend on `sqlite-audit` plus the matching proof summary chain.
- Keep the installed CLI skills surface exact: `tests/check_cli_surface.sh` must report all 14 database items matching `src/plugin.zig` in both `sa skills` text output and `sa skills --json` after dev install.
- Use `zig build sqlite-audit` after build/test/install/recovery/ABI/benchmark-build wiring changes as the single non-benchmark audit target; it should stay green before trusting any higher-level SQLite parity claim. Its `bench` dependency is build-only and does not replace `bench-compare-proof` performance evidence.
- Use `zig build bench-compare-proof` after benchmark/build wiring changes as the broad proof target: configured-run disk + memory reports plus strict configured-run concurrent insert (default 7 runs), ending with a `runs=N` final summary after the protected artifact guard, without pretending disk combined append chain is stable enough to be a default hard gate.
- Use `zig build sqlite-proof` when a single broad command is needed: it runs `sqlite-audit` plus the configured-run disk/memory/concurrent proof (default 7 runs) and then prints a final pass summary with the actual proof run count as `runs=N`. This is evidence for current readiness, not a claim of full SQLite parity.
- Use `zig build bench-compare-disk-strict-chain` and `zig build bench-compare-memory-strict-chain` for narrower opt-in combined append-chain evidence when full proof is unnecessary; disk can fail on variance and memory is the more stable control. Both narrow strict-chain targets print `runs=N` final pass summaries only after their post-run protected artifact guards complete.
- Use `zig build bench-compare-proof-strict-chain` or `zig build sqlite-proof-strict-chain` only when intentionally collecting broad upgrade evidence for making combined append chain a hard gate; these targets print the configured sample count as `runs=N`, can fail on disk variance, and must not replace the conservative default proof until repeated 7-run samples remain stable.
- Use `zig build bench-compare -Dbench-compare-strict-chain=true` as an explicit stress check. It now has a passing 7-run sample after coltx generation reuse, but keep it opt-in until repeated runs show a durable margin over SQLite.
- Use `zig build bench-compare-concurrent-strict-insert` as the explicit opt-in strict correctness/performance gate for concurrent insert until repeated runs justify making it default; use `-Dbench-compare-proof-runs=N` to collect multi-run evidence. The older `zig build bench-compare-concurrent -Dbench-compare-strict-concurrent-insert=true` path remains equivalent for ad hoc checks.
- When judging whether combined append can graduate to a default hard gate, use the printed median margin and min/max range from at least 7-run `bench-compare` samples, not a single 3-run pass.
- Keep `tests/smoke_installed.sh` runnable standalone and from `build.zig`.
- Keep `SA_PLUGINS_HOME` absolute when the build or smoke harness changes directories.
- Keep shared `install-smoke` / `sqlite-audit` access to `.zig-cache/db-install-smoke-home` serialized; concurrent CI/manual audit runs must not observe half-installed plugin state.
- Keep shared `bench` / `bench-compare-*` access to `.zig-cache/db-bench-compare-home` serialized; concurrent benchmark/audit runs must not observe half-installed plugin state or stale SA benchmark linkage.
- Keep benchmark comparison execution and `check-benchmark-artifacts` serialized with `.zig-cache/db-benchmark-run.lock`; concurrent compare runs must not share mutable benchmark roots without the lock.
- Keep all shared `flock` calls bounded with `-Dlock-wait-seconds=N` / `flock -w`; a stuck peer process should produce an explicit build failure instead of an unbounded wait, and `check-bounded-locks` should fail immediately if future `build.zig` edits add an unbounded lock.
- Update `progress.md` with concrete benchmark numbers and regression notes after each meaningful performance change.
