# sa_plugin_db Progress

## Tracking Basis

- Scope: `db` plugin manifest, CLI command surface, qmod registry/execution, and local table storage helpers.
- Progress rule: update this file, including percentages, after each completed feature.
- Current status: `16 / 16` original qmod/CLI features completed (`100.0%`). Since that milestone, the plugin has moved into a development ABI phase with `db.sai` and `db.sal` exported through `sap.json` for SA-facing read handles, typed queries, row writes, dictionaries, blob stores, and ERP planner helpers. Row-list intersection, union, exclusion, candidate text/blob filtering, indexed two-predicate planners for `u64+i64`, `u64+u64`, `i64+i64`, `u64+blob_eq`, and `i64+blob_eq`, plus three-predicate planners for `u64+i64+blob_eq`, `u64+i64+bool`, `u64+i64+i64`, and semantic `u64+date+decimal_i64`, are now exposed for indexed AND/OR/not-in, text-search, and common ERP amount/date/status/business-key/flag composition. Strict fixed-width row update/upsert/delete by unique `u64`, `u8`, `i8`, `u16`, `i16`, `u32`, `i32`, and `i64` keys is available both directly and inside single-table transactions, and read handles can copy full rows by unique typed integer keys (`u64`, `i64`, `u32`, `i32`, `u8`, `i8`, `u16`, `i16`), unique `(u64, u64)` / `(u64, i64)` composite keys, and unique `blob_eq` text/blob keys for ERP form loads. Read handles now provide direct whole-column sums for both unsigned `u64` quantities and signed `i64`/scaled-decimal amounts, plus stable first-seen `u64` group counts and signed `i64` sums for full snapshots or filtered candidate row lists, including grouped `count/sum/min/max` pages and sorted pages by key/count/sum/min/max for Top N ERP reports. Unique `(u64, u64)` plus unique `(u64, i64)` composite-key upsert/update/delete is now available for ERP child rows and signed date/timestamp second-key shapes such as order lines, product/warehouse balances, customer/order-day lists, and status/due-day lists. Unique `blob_eq` text/blob business keys now support direct and transaction upsert/update/delete, with row `blob_handle` validation against the exact key bytes. Transaction-scoped dictionary intern is available through `sa_db_tx_dict_intern` / `DB_TX_DICT_INTERN`, allowing ERP status/category labels and the rows that reference them to commit or roll back together.

## Feature Progress

- Feature 1 - `sap.json` plugin manifest and permissions: 100% (`db` artifact, `database` skill, project-scoped fs permissions, no net/env/process-spawn permissions).
- Feature 2 - Runtime descriptor and CLI command handler: 100% (`database` skill section exported through the runtime descriptor).
- Feature 3 - `db init <schema.sadb-schema>` schema bootstrap: 100% (emits generated `.sai` schema interface files).
- Feature 4 - `db register <query.sa>` qmod registry: 100% (SHA-256 payload storage and declared DB grant validation).
- Feature 5 - `db inspect <table|hash>` / `db status <table>` metadata inspection: 100% (table and qmod registry metadata reporting).
- Feature 6 - `db exec <hash> [--params <file>]` supported qmod execution: 100% for current recognized scalar/read/write/read-write/atomic cursor instruction shapes.
- Feature 7 - `db ingest <table> <csv|jsonl>` table ingestion: 100% (CSV and JSONL append into column segment files).
- Feature 8 - `db snapshot <table>` and `db restore <table> <epoch>` snapshots: 100% (metadata, schema, and segment snapshot/restore).
- Feature 9 - `db verify <table>` integrity checks: 100% (schema hash, segment hashes, row counts, and segment sizes).
- Feature 10 - `db compact <table>` segment compaction: 100% (many segments merged into one active segment).
- Feature 11 - `db lock <table>` table lock: 100% (locked metadata plus readonly file modes).
- Feature 12 - `db unlock <table>` table unlock: 100% (validates current table, clears locked state, restores writable file modes, and allows ingestion again). Verified with `zig build test` on 2026-06-08.
- Feature 13 - Installed-plugin smoke verification: 100% (`tests/smoke_installed.sh` builds, dev-installs, verifies `database` skills, and runs `sa db init/ingest/verify/lock/unlock/verify`). Verified on 2026-06-08.
- Feature 14 - Command-level qmod integration examples: 100% (`docs/qmod_examples.md` documents `sa db register` plus `sa db exec`, and `tests/smoke_installed.sh` verifies register/inspect/exec against a dev-installed plugin). Verified on 2026-06-08.
- Feature 15 - Interface policy: superseded after the original qmod milestone. `sap.json` now exports plugin-shipped `db.sai` and `db.sal`; generated table `.sai` files remain per-project schema outputs.
- Feature 16 - Broader qmod execution surface: 100% (added `and`, `or`, `xor`, `shl`, and `lshr` integer binary ops to the common qmod evaluator path; covered by scalar and read-only DB qmod unit tests plus installed-plugin smoke). Verified on 2026-06-08.

## Current Verification

- The current configured-run proof is `zig build bench-compare-proof -Dlock-wait-seconds=900` from 2026-07-07. Disk indexed ERP passed hard stages with total append chain db `3.726 ms [3.485, 3.887]` vs SQLite `3.934 ms [3.539, 4.185]` (`0.207 ms` median margin, still report-only by default). Memory indexed ERP total append chain passed at db `2.313 ms [2.195, 2.903]` vs SQLite `3.325 ms [3.017, 4.625]`. Concurrent strict insert passed with raw db `5.986 ms [5.599, 6.981]` vs SQLite `80.419 ms [68.939, 100.006]`, `db_ok=7/7`, and coltx db `7.979 ms [6.896, 10.895]` vs SQLite `80.419 ms [68.939, 100.006]`, `db_ok=7/7`. The command ended with protected benchmark artifacts clean and `bench-compare-proof passed: runs=7`.
- A later accidental text-search command used shell backticks incorrectly and triggered `zig build sqlite-proof`; that proof run failed the memory indexed ERP init hard gate by a tiny margin: db `0.544 ms` vs SQLite `0.534 ms`. `zig build check-benchmark-artifacts -Dlock-wait-seconds=1` passed immediately afterward. This remains useful historical variance evidence, not a code deadlock; full SQLite parity remains unproven until repeated current proof runs cover memory and durable disk paths with the existing caveats.
- On 2026-07-07, memory-root schema bootstrap stopped eagerly writing the schema file into the in-memory filesystem. `initMemorySchemaDeferred` now keeps schema bytes in the deferred/init caches, and `ensureDeferredSchemaMaterialized` writes the schema only when file-shaped readers need it (`loadSchema`, schema hashing, snapshot artifact copy, segment verification, or recovery validation). Regression coverage proves `:memory:` init and first row write leave the schema file absent and `verifyTable` materializes it on demand. Verified with `zig test src/table.zig --test-filter 'table memory root defers schema file materialization until verification'`, `zig test src/table.zig --test-filter 'memory root'`, `zig test src/db_saasm_api.zig`, and `zig build check-benchmark-hot-io-guards check-docs check-proof-wiring -Dlock-wait-seconds=1`. The latest `zig build bench-compare-memory -Dbench-compare-runs=7 -Dlock-wait-seconds=900` passed with init db `0.226 ms [0.198, 0.277]` vs SQLite `0.472 ms [0.421, 0.527]`, baseline db `2.657 ms [2.379, 3.163]` vs SQLite `57.643 ms [54.816, 81.741]`, build db `6.542 ms [6.394, 8.316]` vs SQLite `19.120 ms [15.947, 22.734]`, tx append db `1.313 ms [1.258, 1.744]` vs SQLite `3.287 ms [2.984, 3.941]`, coltx append db `0.888 ms [0.812, 1.126]` vs SQLite `3.287 ms [2.984, 3.941]`, verify db `0.190 ms [0.178, 0.279]` vs SQLite `33.012 ms [30.594, 34.134]`, and total append chain db `2.227 ms [2.111, 2.870]` vs SQLite `3.287 ms [2.984, 3.941]`. This addresses the memory-init hot-path variance sample only; disk combined append remains report-only and this is not a full SQLite parity claim.
- On 2026-07-07, unsafe/no-sync memory-root writes stopped materializing deferred schema through compat meta publication. `writeCompatMeta` now skips `ensureDeferredSchemaMaterialized` for memory roots while preserving disk no-sync schema materialization, and the new regression `table unsafe memory root write paths defer schema file materialization until verification` covers direct row insert, write-transaction commit, and coltx commit all leaving the memory schema file absent until `verifyTable` materializes it. `tests/check_hot_io_guards.sh` now requires the `!isMemoryRoot(root_dir)` boundary and this unsafe memory-root direct/tx/coltx coverage. Verified with `zig test src/table.zig --test-filter 'table unsafe memory root write paths defer schema file materialization until verification'`, `bash -n tests/check_hot_io_guards.sh`, `zig build check-benchmark-hot-io-guards -Dlock-wait-seconds=1`, `zig test src/table.zig`, `zig test src/db_saasm_api.zig`, and `zig build bench-compare-memory -Dbench-compare-runs=3 -Dlock-wait-seconds=900`. The 3-run memory compare passed with init db `0.286 ms [0.223, 0.395]` vs SQLite `0.524 ms [0.429, 0.606]`, baseline db `3.000 ms [2.315, 3.078]` vs SQLite `59.330 ms [57.360, 66.407]`, build db `7.252 ms [6.326, 8.664]` vs SQLite `19.590 ms [18.585, 19.783]`, tx append db `1.269 ms [1.252, 1.525]` vs SQLite `3.639 ms [3.237, 3.881]`, coltx append db `0.958 ms [0.857, 1.091]` vs SQLite `3.639 ms [3.237, 3.881]`, verify db `0.231 ms [0.198, 0.300]` vs SQLite `36.452 ms [32.968, 47.203]`, and total append chain db `2.227 ms [2.109, 2.616]` vs SQLite `3.639 ms [3.237, 3.881]`. This removes no-sync memory write-path schema I/O only; verification, snapshot, recovery, `loadSchema`, and schema hashing still materialize schema on demand, and this is not a full SQLite parity claim.
- On 2026-07-07, `build.zig` added `zig build check-bounded-locks` and made `zig build test` depend on it. The new bounded lock guard scans every `build.zig` `"flock"` call and rejects any call that lacks `"-w", lock_wait_seconds_arg`, so future install-smoke, ABI-smoke, benchmark-build, benchmark-compare, or protected-artifact lock edits cannot silently reintroduce an unbounded wait. `sqlite-audit` now reports the bounded lock guard in its final summary, while `check-docs` and `check-proof-wiring` require the target, script, dependency, and summary text to stay wired. This is build/proof liveness hardening only and is not proof of full SQLite parity.
- On 2026-07-07, direct blob append stopped rebuilding blob indexes when the appended blob id is not referenced by any existing row. `putBlobValue` now calls `blobStoreAppendRequiresIndexRebuild`, which scans mapped blob-handle segment columns only for indexes on the target store; it preserves the future-id correctness case by rebuilding when an existing row already points at the newly appended id, but keeps `blob_eq`/token/prefix/contains index artifacts unchanged for unreferenced blob appends. Regression coverage `table blob append skips index rebuild until appended blob is referenced` proves the index path/hash/bytes stay stable after an unreferenced append and that a later inserted row referencing the blob is still findable through append-index maintenance. Verified with `zig test src/table.zig --test-filter 'table blob append skips index rebuild until appended blob is referenced'`, `zig test src/table.zig --test-filter 'table blob exact index is rebuilt when blob values are appended'`, `zig build check-benchmark-hot-io-guards check-docs -Dlock-wait-seconds=1`, `zig test src/table.zig`, `zig test src/db_saasm_api.zig`, `zig build check-benchmark-hot-io-guards check-docs check-proof-wiring check-bounded-locks -Dlock-wait-seconds=1`, `git diff --check`, and `zig build bench-compare-disk -Dbench-compare-runs=3 -Dlock-wait-seconds=900`. The disk compare passed all hard stages with init db `0.180 ms [0.169, 0.214]` vs SQLite `0.823 ms [0.745, 0.929]`, baseline db `4.902 ms [4.630, 5.018]` vs SQLite `66.791 ms [62.745, 70.757]`, build db `9.017 ms [8.587, 9.507]` vs SQLite `20.693 ms [20.502, 21.832]`, tx append db `2.416 ms [2.362, 2.638]` vs SQLite `4.220 ms [3.895, 6.441]`, coltx append db `1.653 ms [1.596, 1.853]` vs SQLite `4.220 ms [3.895, 6.441]`, and verify db `0.953 ms [0.930, 0.985]` vs SQLite `33.774 ms [32.509, 36.146]`. Total append chain remained report-only WARN at db `4.269 ms [3.958, 4.291]` vs SQLite `4.220 ms [3.895, 6.441]`, so this was hidden rebuild removal only: at that point blob append still built a replacement blob artifact buffer, disk strict-chain was not graduated, and this was not proof of full SQLite parity.
- On 2026-07-07, dictionary/blob artifact writes stopped doing a redundant parent-directory fsync before the later meta/manifest publication points at them. `flushPendingDictWrites`, `flushPendingBlobWrites`, unsafe cached dict/blob materialization in `writeCompatMeta`, direct `internStringDict`, direct `internStringDictMany`, and direct `putBlobValue` now use `writeArtifactFile`, while meta, manifest, transaction marker, and segment publication sync boundaries stay unchanged. `tests/check_hot_io_guards.sh` now locks those artifact-write call sites. Verified with `zig fmt src/table.zig`, `bash -n tests/check_hot_io_guards.sh`, `zig build check-benchmark-hot-io-guards -Dlock-wait-seconds=1`, `zig test src/table.zig --test-filter 'table string dictionary is persisted snapshotted restored and verified'`, `zig test src/table.zig --test-filter 'table intern string dict many batches inserts and reuses ids'`, `zig test src/table.zig --test-filter 'table write transaction interns dictionaries with rows atomically'`, `zig test src/table.zig --test-filter 'table write transaction commits blob handles with rows atomically'`, `zig test src/table.zig --test-filter 'table blob append skips index rebuild until appended blob is referenced'`, `zig test src/table.zig`, `zig test src/db_saasm_api.zig`, and `zig build bench-compare-disk -Dbench-compare-runs=3 -Dlock-wait-seconds=900`. The disk compare passed all hard stages with init db `0.197 ms [0.176, 0.255]` vs SQLite `0.986 ms [0.742, 0.993]`, baseline db `4.697 ms [4.445, 4.883]` vs SQLite `78.009 ms [68.986, 82.123]`, build db `9.241 ms [9.136, 9.591]` vs SQLite `20.688 ms [18.191, 26.694]`, tx append db `2.342 ms [2.257, 2.434]` vs SQLite `4.426 ms [3.947, 6.190]`, coltx append db `1.531 ms [1.518, 1.684]` vs SQLite `4.426 ms [3.947, 6.190]`, verify db `1.034 ms [0.919, 1.081]` vs SQLite `45.456 ms [34.862, 50.361]`, and total append chain db `3.860 ms [3.788, 4.118]` vs SQLite `4.426 ms [3.947, 6.190]`. This was a 3-run durable-path sample only: at that point replacement-buffer blob/dictionary append behavior was unchanged, disk strict-chain was not graduated, and this was not proof of full SQLite parity.
- On 2026-07-07, batched string dictionary intern stopped rescanning the existing dictionary once per input value. `dictScanCountAndFindValueIds` now validates existing dictionary bytes once while filling `out_ids` for all requested values; durable `internStringDictMany` and unsafe bootstrap `unsafeInitCacheInternStringDictMany` then test `out_ids[idx] != 0` before handling pending values and same-batch duplicates. `tests/check_hot_io_guards.sh` now requires the helper and forbids `dictFindValueId` in both batched intern paths. Verified with `zig fmt src/table.zig`, `bash -n tests/check_hot_io_guards.sh`, `zig build check-benchmark-hot-io-guards -Dlock-wait-seconds=1`, `zig test src/table.zig --test-filter 'table intern string dict many batches inserts and reuses ids'`, `zig test src/table.zig --test-filter 'table unsafe init dict bootstrap reuses cache across batches without materialization'`, `zig test src/table.zig`, `zig test src/db_saasm_api.zig`, and `zig build bench-compare-disk -Dbench-compare-runs=3 -Dlock-wait-seconds=900`. The disk compare passed hard stages with init db `0.186 ms [0.179, 0.332]` vs SQLite `0.811 ms [0.728, 5.044]`, baseline db `4.533 ms [4.280, 4.737]` vs SQLite `65.304 ms [62.307, 73.728]`, build db `9.512 ms [8.221, 10.523]` vs SQLite `21.732 ms [17.975, 25.688]`, tx append db `2.801 ms [2.404, 3.856]` vs SQLite `4.160 ms [3.788, 5.839]`, coltx append db `1.757 ms [1.643, 2.006]` vs SQLite `4.160 ms [3.788, 5.839]`, and verify db `1.004 ms [0.823, 1.211]` vs SQLite `35.089 ms [33.970, 38.055]`. Total append chain remained report-only WARN at db `4.807 ms [4.047, 5.613]` vs SQLite `4.160 ms [3.788, 5.839]`, so this removed repeated old-dictionary scans in batched intern only: at that point replacement-buffer dictionary append behavior was unchanged, disk strict-chain was not graduated, and this was not proof of full SQLite parity.
- On 2026-07-07, direct durable dictionary/blob append stopped constructing full replacement artifact buffers. `makeCountedArtifactHashes` now computes file and block hashes over the counted-artifact stream (`new-count`, old payload without the old count header, and appended `len+value` entries), while `writeCountedArtifactFile` writes the same stream through a temp artifact before meta publication. Durable direct `internStringDict`, `internStringDictMany`, and `putBlobValue` use the streaming helpers; transaction pending dictionary/blob writes, unsafe bootstrap cache writes, and memory-root fallback writes still keep full artifact bytes because they must serve uncommitted or cached read views. `tests/check_hot_io_guards.sh` now requires the streaming helpers in those direct paths and forbids replacement-buffer `writeArtifactFile(new_bytes)` regressions there. Verified with `zig fmt src/table.zig`, `bash -n tests/check_hot_io_guards.sh`, `zig build check-benchmark-hot-io-guards -Dlock-wait-seconds=1`, `zig test src/table.zig --test-filter 'table intern string dict many batches inserts and reuses ids'`, `zig test src/table.zig --test-filter 'table blob append skips index rebuild until appended blob is referenced'`, `zig test src/table.zig`, `zig test src/db_saasm_api.zig`, and `zig build bench-compare-disk -Dbench-compare-runs=3 -Dlock-wait-seconds=900`. The disk compare passed hard stages with init db `0.177 ms [0.167, 0.313]` vs SQLite `1.122 ms [0.819, 4.734]`, baseline db `4.187 ms [4.132, 4.490]` vs SQLite `66.022 ms [58.541, 74.628]`, build db `8.532 ms [8.332, 9.632]` vs SQLite `19.089 ms [18.914, 29.781]`, tx append db `2.293 ms [2.250, 2.327]` vs SQLite `4.476 ms [3.615, 6.353]`, coltx append db `1.613 ms [1.596, 1.627]` vs SQLite `4.476 ms [3.615, 6.353]`, verify db `0.840 ms [0.833, 0.984]` vs SQLite `36.702 ms [31.872, 37.362]`, and total append chain db `3.889 ms [3.863, 3.954]` vs SQLite `4.476 ms [3.615, 6.353]`. This is still a 3-run durable-path sample: disk strict-chain remains opt-in and this is not proof of full SQLite parity.
- On 2026-07-07, unsafe/no-sync in-place index merge stopped doing a redundant second full validation pass after rewriting the mmap/memory buffer. `mergeSingleIndexBytesInPlace` and `mergeU64PairIndexBytesInPlace` now validate exact byte-count, row range, sorted order, unique-key conflicts, and variable blob-text duplicate shape while merging. Regression coverage `table unsafe in-place index merge validates while merging` proves valid single/pair merges still work and corrupt ordering, duplicate variable entries, and row-count byte mismatches still fail. Verified with `zig test src/table.zig --test-filter 'table unsafe in-place index merge validates while merging'`, `zig test src/table.zig`, and `zig test src/db_saasm_api.zig`. A follow-up `zig build bench-compare-disk -Dbench-compare-runs=3 -Dlock-wait-seconds=900` passed all hard stages with init db `0.187 ms [0.166, 0.289]` vs SQLite `0.736 ms [0.722, 6.772]`, baseline db `4.411 ms [4.168, 5.385]` vs SQLite `63.749 ms [62.828, 64.861]`, build db `10.839 ms [8.089, 10.845]` vs SQLite `18.038 ms [17.271, 20.979]`, tx append db `2.786 ms [2.243, 3.237]` vs SQLite `3.782 ms [3.637, 4.035]`, coltx append db `2.050 ms [1.446, 2.090]` vs SQLite `3.782 ms [3.637, 4.035]`, verify db `1.130 ms [0.868, 1.197]` vs SQLite `34.073 ms [32.686, 35.017]`, and protected benchmark artifacts clean. Total append chain remained report-only WARN at db `4.835 ms [3.689, 5.328]` vs SQLite `3.782 ms [3.637, 4.035]`, so this is a hot-path validation simplification, not a strict-chain graduation or full SQLite parity claim.
- An earlier `SA_DB_UNSAFE_NO_SYNC=1` 5-run `db_erp_indexed_write_bench` sample from clean roots, after making unsafe/no-sync active/writable meta loads prefer the hot meta cache and keeping transaction commits cache-warm, showed db medians of `db_erp_indexed_init_ns=0.174 ms`, `baseline=4.490 ms`, `build=8.305 ms`, `tx append=2.242 ms`, `coltx append=1.564 ms`, `total append chain=3.806 ms`, and `verify=0.988 ms`, versus SQLite medians of `init=0.798 ms`, `baseline=77.058 ms`, `build=21.115 ms`, `append=4.086 ms`, and `verify=35.944 ms`.
- An earlier `SA_DB_UNSAFE_NO_SYNC=1` 5-run `db_erp_indexed_memory_bench` sample from clean roots showed db medians of `db_erp_indexed_memory_init_ns=0.352 ms`, `baseline=3.223 ms`, `build=6.826 ms`, `tx append=1.442 ms`, `coltx append=0.981 ms`, `total append chain=2.538 ms`, and `verify=0.273 ms`, versus SQLite medians of `init=0.578 ms`, `baseline=62.296 ms`, `build=18.644 ms`, `append=3.389 ms`, and `verify=33.654 ms`.
- Raw samples for that earlier rerun are in `benchmark_test/db_erp_indexed_write_bench_runs.txt`, `benchmark_test/sqlite_erp_indexed_write_bench_runs.txt`, `benchmark_test/db_erp_indexed_memory_bench_runs.txt`, and `benchmark_test/sqlite_erp_indexed_memory_bench_runs.txt`.
- On 2026-07-06, unsafe/no-sync `loadActiveMeta` and `loadWritableMeta` now prefer the process-local unsafe init/meta cache before reparsing compat meta from disk, and unsafe write-transaction commit refreshes that cache instead of deleting it. The regression coverage checks that an unsafe transaction append leaves a cache entry with the committed row count, epoch, and schema hash. This removed redundant meta reads/parses from hot transaction, coltx, and verify paths in the indexed ERP write benchmark.
- On 2026-07-06, `build.zig` now installs `libdb.so`, `db.sai`, and `db.sal`, checks `benchmark_test` interface copies, runs an isolated `install-smoke` step with absolute `SA_PLUGINS_HOME`, and builds both db benchmarks and SQLite control benchmarks through `bench`. The SQLite control path compiles SA sources with `sa build-obj`, rebuilds a local `libsa_std_no_sqlite_stub.a` with std SQLite stubs renamed, links the configured system SQLite library, and supports `-Dsqlite-lib=...` / `-Dsa-std-lib=...`. `tests/smoke_installed.sh` now accepts `SA_PLUGIN_DB_SKIP_BUILD`, `SA_PLUGIN_DB_SA_BIN`, and `SA_PLUGIN_DB_SMOKE_HOME`. At that point memory-root bootstrap materialized the schema into the in-memory filesystem so `verify`, `snapshot`, and `recover` worked without a backing directory; this was superseded on 2026-07-07 by on-demand memory schema materialization. `zig build`, `zig build test`, and `zig build bench` all pass.
- On 2026-07-06, `zig build bench-compare` was added as the default SQLite-control gate for the indexed ERP write benchmark. It installs the dev plugin into an isolated `.zig-cache/db-bench-compare-home`, validates matching row counts, runs db samples from the repository root, and runs SQLite control samples from a temporary cwd so tracked SQLite benchmark artifacts are not touched. The default gate hard-fails init, baseline ingest, index build, `tx` append, `coltx` append, and verify when db is not faster than SQLite; the combined append chain is reported by default and only hard-fails with `-Dbench-compare-strict-chain=true` because earlier strict-chain runs exposed small-margin variance. The current default run passed with 3-run medians: init `0.237 ms` vs SQLite `0.718 ms`, baseline `5.284 ms` vs `67.957 ms`, build `9.730 ms` vs `22.112 ms`, `tx` append `2.459 ms` vs `4.793 ms`, `coltx` append `1.631 ms` vs `4.793 ms`, verify `0.981 ms` vs `39.080 ms`, and total append chain `4.091 ms` vs `4.793 ms`.
- On 2026-07-06, `build.zig` extended `bench-compare` into a disk-plus-memory SQLite-control gate and added `bench-compare-memory` for memory-only runs. The shared compare script is now prefix-driven, so the same row-count and median checks cover `db_erp_indexed_*` against `sqlite_erp_indexed_*` and `db_erp_indexed_memory_*` against `sqlite_erp_indexed_memory_*`. The latest default `zig build bench-compare` passed all hard gates. Disk 3-run medians were init `0.226 ms` vs SQLite `0.917 ms`, baseline `5.160 ms` vs `71.985 ms`, build `10.061 ms` vs `24.657 ms`, `tx` append `2.850 ms` vs `4.627 ms`, `coltx` append `2.060 ms` vs `4.627 ms`, verify `1.277 ms` vs `40.305 ms`, and total append chain `4.910 ms` vs `4.627 ms` as a default report-only `WARN`. Memory 3-run medians were init `0.471 ms` vs SQLite `0.563 ms`, baseline `3.606 ms` vs `68.236 ms`, build `7.912 ms` vs `22.331 ms`, `tx` append `1.369 ms` vs `3.933 ms`, `coltx` append `1.001 ms` vs `3.933 ms`, verify `0.283 ms` vs `38.762 ms`, and total append chain `2.455 ms` vs `3.933 ms`.
- On 2026-07-06, `bench-compare` diagnostics now print `median [min,max]` plus median margin for each stage. A 7-run `zig build bench-compare -Dbench-compare-runs=7` passed all hard gates and reported disk medians/ranges of init `0.247 ms [0.181, 0.444]` vs SQLite `0.836 ms [0.724, 1.126]`, baseline `4.687 ms [4.267, 6.031]` vs `61.442 ms [60.006, 80.417]`, build `8.815 ms [8.273, 11.206]` vs `20.216 ms [18.884, 22.377]`, `tx` append `2.214 ms [2.132, 2.771]` vs `4.220 ms [3.540, 4.600]`, `coltx` append `1.604 ms [1.509, 2.124]` vs `4.220 ms [3.540, 4.600]`, verify `0.917 ms [0.866, 1.201]` vs `35.596 ms [33.054, 49.717]`, and total append chain `3.814 ms [3.641, 4.895]` vs `4.220 ms [3.540, 4.600]` with `0.406 ms` median margin. Memory 7-run medians/ranges were init `0.339 ms [0.261, 0.420]` vs SQLite `0.462 ms [0.405, 5.938]`, baseline `2.802 ms [2.448, 3.600]` vs `59.587 ms [54.905, 90.905]`, build `7.129 ms [6.568, 8.376]` vs `18.574 ms [16.735, 23.328]`, `tx` append `1.694 ms [1.280, 1.758]` vs `3.415 ms [3.264, 3.817]`, `coltx` append `0.968 ms [0.794, 1.258]` vs `3.415 ms [3.264, 3.817]`, verify `0.268 ms [0.181, 0.319]` vs `33.894 ms [30.606, 36.690]`, and total append chain `2.662 ms [2.074, 2.976]` vs `3.415 ms [3.264, 3.817]` with `0.753 ms` median margin. The positive disk chain median does not change the default gate policy because the min/max ranges still overlap.
- On 2026-07-06, `build.zig` added `bench-compare-concurrent` and included it in default `bench-compare`. The concurrent gate backs up and restores tracked `.bench_raw`, `.bench_coltx`, and `.bench_sqlite` roots, hard-fails serial/concurrent query correctness and median regressions, and reports concurrent insert correctness/performance by default. Current 3-run default `bench-compare` passed the indexed gates plus concurrent query gates: best serial query `3.850 ms [3.838, 4.011]` vs SQLite `329.901 ms [314.287, 348.408]`, and best concurrent query `4.504 ms [4.058, 6.382]` vs SQLite `88.207 ms [86.392, 111.835]`. Concurrent insert remains a real unresolved gap: raw insert reported `3.316 ms [3.164, 6.222]` vs SQLite `101.249 ms [75.421, 101.560]` but `db_ok=0/3`, and coltx insert reported `3.804 ms [3.326, 5.025]` vs SQLite `101.249 ms [75.421, 101.560]` but `db_ok=0/3`. `zig build bench-compare-concurrent -Dbench-compare-strict-concurrent-insert=true` intentionally fails until that correctness gap is fixed.
- On 2026-07-06, concurrent insert correctness was fixed for both raw `sa_db_ingest_columns` and `sa_db_coltx_*` benchmark paths. Unsafe/no-sync and memory-root process write locks now wait briefly instead of immediately returning `Locked`, so concurrent writers serialize like a SQLite busy-timeout path. Column ingest sessions no longer hold a write lock or full `TableMeta` from `begin` through `commit`; they keep only schema-shape validation data, acquire the write lock at commit, reload the latest writable meta, and append staged batches against current row counts. A regression test now opens two unsafe coltx sessions before either commits and verifies both commits are preserved. `build.zig` also forces SA db benchmark compilation to use the isolated `.zig-cache/db-bench-compare-home` dev install, preventing stale global `libdb.so` linkage. Verified with `zig test src/table.zig`, `zig build`, `zig build test`, `zig build bench`, `zig build bench-compare`, `zig build bench-compare-memory`, and `zig build bench-compare-concurrent -Dbench-compare-strict-concurrent-insert=true`. The strict concurrent 3-run gate passed: raw insert `6.615 ms [5.860, 6.676]` vs SQLite `102.345 ms [75.384, 116.810]`, `db_ok=3/3`; coltx insert `7.445 ms [7.091, 9.493]` vs SQLite `102.345 ms [75.384, 116.810]`, `db_ok=3/3`. The same run kept serial/concurrent query gates ahead of SQLite: best serial `3.901 ms [3.807, 4.897]` vs `302.068 ms [291.027, 320.815]`, best concurrent `4.359 ms [3.190, 4.611]` vs `97.166 ms [85.250, 120.672]`.
- On 2026-07-06 after the concurrent insert fix, the latest default `zig build bench-compare` passed all hard gates. Disk indexed ERP medians were init `0.207 ms [0.191, 0.215]` vs SQLite `0.863 ms [0.743, 0.932]`, baseline `4.908 ms [4.734, 5.240]` vs `62.420 ms [62.366, 63.855]`, build `9.778 ms [8.837, 10.207]` vs `21.922 ms [20.672, 23.018]`, `tx` append `2.560 ms [2.288, 2.703]` vs `4.270 ms [4.021, 4.813]`, `coltx` append `2.001 ms [1.783, 3.035]` vs `4.270 ms [4.021, 4.813]`, verify `1.189 ms [0.936, 1.279]` vs `33.855 ms [32.956, 38.301]`, and report-only total append chain `4.562 ms [4.071, 5.738]` vs `4.270 ms [4.021, 4.813]`. Memory-only `zig build bench-compare-memory` passed with total append chain `2.514 ms [2.481, 2.553]` vs SQLite `3.483 ms [3.195, 4.172]`. The disk chain remains report-only because the latest 3-run median is behind SQLite even though the individual tx and coltx append sub-stages pass.
- On 2026-07-06, coltx commit regained the no-contention hot path without reverting the concurrent insert correctness fix. Unsafe meta cache entries now carry a process-local generation. `beginColumnIngestSession` stores an owned meta snapshot plus the generation observed at begin; `commitColumnIngestSession` acquires the write lock and reuses that snapshot only if the generation is unchanged, otherwise it reloads current writable meta before appending. This preserves the previous concurrent-session correctness while avoiding an extra meta load/clone for the common single-session coltx append path. Verified with `zig test src/table.zig`, `zig test src/db_saasm_api.zig`, `zig build`, `zig build test`, `zig build bench`, `zig build bench-compare -Dbench-compare-strict-chain=true`, `zig build bench-compare -Dbench-compare-runs=7 -Dbench-compare-strict-chain=true`, and `zig build bench-compare-concurrent -Dbench-compare-strict-concurrent-insert=true`.
- The latest 7-run strict-chain sample passed disk, memory, and concurrent gates. Disk indexed ERP medians/ranges were init `0.204 ms [0.182, 0.254]` vs SQLite `0.990 ms [0.735, 1.190]`, baseline `5.052 ms [4.679, 5.209]` vs `76.172 ms [67.119, 96.887]`, build `9.885 ms [8.695, 10.075]` vs `26.210 ms [21.082, 32.033]`, `tx` append `2.468 ms [2.168, 2.741]` vs `5.094 ms [4.182, 6.790]`, `coltx` append `1.802 ms [1.717, 1.839]` vs `5.094 ms [4.182, 6.790]`, verify `1.010 ms [0.916, 1.236]` vs `43.100 ms [35.644, 46.177]`, and total append chain `4.241 ms [3.921, 4.543]` vs SQLite `5.094 ms [4.182, 6.790]` with `0.853 ms` median margin. Memory total append chain was `2.430 ms [2.242, 2.653]` vs SQLite `4.230 ms [3.896, 5.854]` with `1.800 ms` margin. Concurrent insert also stayed correct across 7 runs: raw `6.648 ms [5.022, 7.394]`, coltx `8.090 ms [6.748, 10.086]`, SQLite `97.265 ms [73.542, 109.421]`, raw/coltx both `db_ok=7/7`. The strict chain and strict insert policies remain opt-in until repeated samples confirm the margin is stable enough for default hard gates.
- On 2026-07-06, `build.zig` added `bench-compare-proof` as the broad 7-run proof/report target. It runs disk indexed ERP and memory indexed ERP with `strict_chain=false`, then runs concurrent comparison with `strict_insert=true`; this preserves the policy that disk combined append chain is evidence, not a default hard gate. The target passed: disk total append chain `3.977 ms [3.732, 5.546]` vs SQLite `4.300 ms [4.031, 6.636]`, memory total append chain `2.924 ms [2.209, 3.021]` vs SQLite `3.332 ms [2.977, 3.675]`, best serial query `3.839 ms [3.679, 4.597]` vs SQLite `321.070 ms [288.918, 352.186]`, best concurrent query `4.648 ms [3.610, 5.068]` vs SQLite `107.002 ms [91.706, 137.186]`, raw insert `6.347 ms [5.474, 70.936]` vs SQLite `80.400 ms [70.894, 104.098]` with `db_ok=7/7`, and coltx insert `7.971 ms [6.898, 10.486]` vs SQLite `80.400 ms [70.894, 104.098]` with `db_ok=7/7`.
- On 2026-07-06, explicit snapshot restore was made recovery-stable. `restoreSnapshotArtifacts` now publishes the restored snapshot as a new committed epoch instead of reusing the old snapshot epoch, preventing a later `recover` from selecting a higher pre-restore versioned meta and undoing the restore. The table lifecycle regression now verifies `restore -> recover` keeps the restored row count and epoch, and `tests/smoke_installed.sh` now covers the installed CLI flow `snapshot -> compact -> lock -> failed ingest -> unlock -> ingest -> restore -> recover -> verify`. Verified with `zig test src/table.zig`, `zig test src/db_saasm_api.zig`, `zig build test`, and `zig build`.
- On 2026-07-06, the restore/recover lifecycle proof was lifted to the public SA ABI. Added `benchmark_test/db_lifecycle_smoke.sa`, which uses `db.sal` to remove/init, insert rows, snapshot, compact, lock, verify locked writes return `SA_DB_ERR_LOCKED`, unlock, write again, restore the earlier snapshot, recover, and read back the restored two-row sum through a read handle. `build.zig` now exposes `zig build abi-smoke`, runs that smoke from a temporary cwd against the isolated dev-installed plugin, and makes `zig build test` depend on it. Verified with `zig build abi-smoke`, `zig build test`, and `zig build`.
- On 2026-07-06, public CLI crash-recovery coverage was added with `tests/crash_recover_smoke.sh` and the `zig build crash-recover-smoke` target. The smoke runs from a temporary cwd against the isolated dev-installed plugin, first replaces the active manifest with a stale two-row copy while the newer three-row versioned meta exists and verifies `sa db recover` republishes the three-row commit, then corrupts the latest versioned meta and verifies `recover` falls back to the previous valid two-row commit. `zig build test` now depends on this smoke so manifest/versioned-meta recovery remains covered outside Zig internals. Verified with `zig build crash-recover-smoke`, `zig test src/table.zig`, `zig test src/db_saasm_api.zig`, `zig build test`, and `zig build`.
- On 2026-07-06, `tests/crash_recover_smoke.sh` was extended to cover transaction marker recovery through the installed public CLI. It now writes a valid `flash_sale.tx.<epoch>.commit` marker next to a stale two-row manifest and verifies `sa db recover` completes the marked three-row commit, then writes only `flash_sale.tx.<epoch>.pending` and verifies recovery ignores the uncommitted three-row versioned meta, keeps the two-row committed state, and removes the pending marker. Verified with `bash -n tests/crash_recover_smoke.sh`, `zig build crash-recover-smoke`, `zig build install-smoke`, `zig build test`, and `zig build`.
- On 2026-07-06, `tests/crash_recover_smoke.sh` was extended again to lift corrupt artifact fallback into the installed public CLI gate. It now corrupts the latest committed segment file for `flash_sale` and verifies `recover` skips that otherwise-readable latest meta and republishes the previous two-row commit, then builds an indexed table through `sa db index-u64`, corrupts the latest index artifact, and verifies `recover` falls back to the previous valid data epoch. Verified with `bash -n tests/crash_recover_smoke.sh`, `zig build crash-recover-smoke`, `zig build install-smoke`, `zig build sqlite-audit`, `zig build test`, and `zig build`. A parallel manual run of `install-smoke` and `sqlite-audit` initially contended on the shared `.zig-cache/db-install-smoke-home`; this was fixed on 2026-07-07 by serializing shared home access in `build.zig`.
- On 2026-07-07, `build.zig` added a shared `flock` around `.zig-cache/db-install-smoke-home` access for `sa plugin install --dev`, installed manifest/layout verification, installed CLI text/json surface verification, `tests/smoke_installed.sh`, and `tests/crash_recover_smoke.sh`. This prevents independent `zig build install-smoke` and `zig build sqlite-audit` processes from observing a half-installed plugin state in the shared smoke home. Verified with `zig build install-smoke`, a concurrent `zig build install-smoke` plus `zig build sqlite-audit` run, `zig build test`, and `zig build`.
- On 2026-07-07, `build.zig` added a shared `flock` around `.zig-cache/db-bench-compare-home` for benchmark dev install and every SA db benchmark compile. `abi-smoke` build/run steps now also reuse the install-smoke lock. This prevents concurrent `zig build bench`, `bench-compare-*`, `sqlite-audit`, or `test` invocations from reading a half-installed plugin home or linking/running against stale plugin artifacts. Verified with `zig build bench`, `zig build bench-compare-memory -Dbench-compare-runs=1`, a concurrent `zig build bench` plus `zig build bench-compare-memory -Dbench-compare-runs=1` run, `zig build sqlite-audit`, `zig build test`, and `zig build`. The 1-run `bench-compare-memory` invocations were used only to validate the build/run/concurrency chain and protected artifact guards; they are not new performance proof samples.
- On 2026-07-07, `build.zig` added `.zig-cache/db-benchmark-run.lock` around benchmark comparison execution and `check-benchmark-artifacts`. `bench-compare-memory`, `bench-compare-concurrent`, `bench-compare`, and `bench-compare-proof` now serialize the mutable benchmark roots they clean/restore, and artifact guards cannot inspect those roots while another comparison is mid-run. The compare runners were also hardened to print child benchmark stdout/stderr before reporting a non-zero exit. Verified with `zig build bench-compare-memory -Dbench-compare-runs=1`, `zig build bench-compare-concurrent -Dbench-compare-runs=1`, a concurrent `zig build bench-compare-memory -Dbench-compare-runs=1` plus `zig build bench-compare-concurrent -Dbench-compare-runs=1` run, `zig build sqlite-audit`, `zig build test`, and `zig build`. These 1-run compare invocations were lock/diagnostic-chain verification only, not new performance proof samples.
- On 2026-07-07, all shared `flock` calls in `build.zig` were changed to bounded waits through `flock -w` with a configurable `-Dlock-wait-seconds=N` option, defaulting to `900` seconds. This covers install-smoke, installed layout and CLI checks, crash recovery smoke, dev benchmark install, SA benchmark/ABI smoke builds, ABI smoke execution, benchmark compare runners, and protected artifact guards. `check-proof-wiring` now requires the bounded-wait option and at least the current 12 bounded lock call sites so future edits cannot silently reintroduce unbounded waits. Verified with `zig build check-proof-wiring check-docs -Dlock-wait-seconds=1`, `zig build check-benchmark-parser-guards check-benchmark-hot-io-guards -Dlock-wait-seconds=1`, `zig build -Dlock-wait-seconds=1`, `zig test src/table.zig --test-filter "table write transaction commits blob handles with rows atomically"`, `zig test src/table.zig --test-filter "table append-only transaction key lookup scans pending rows without materializing base"`, `zig test src/db_saasm_api.zig`, `zig build test -Dlock-wait-seconds=1`, `zig build sqlite-audit -Dlock-wait-seconds=1`, and `zig build bench-compare-memory -Dbench-compare-runs=1 -Dlock-wait-seconds=1`. The 1-run memory compare only validates the bounded-lock benchmark runner chain and protected artifact guards; it is not a new stable performance proof sample. This is build/CI liveness hardening only; it does not change SQLite parity claims or performance proof policy.
- On 2026-07-07 after adding benchmark-run serialization, `zig build bench-compare-proof` passed the broad 7-run proof/report target and confirmed the lock wiring does not break benchmark execution or artifact guards. Disk indexed ERP hard stages passed, but the report-only total append chain was behind SQLite this time: db `4.596 ms [3.925, 4.734]` vs SQLite `4.282 ms [3.750, 5.799]`, so disk combined append remains explicitly non-strict. Memory total append chain passed at db `2.799 ms [2.302, 3.317]` vs SQLite `4.232 ms [3.383, 5.229]`. Concurrent strict insert stayed correct and faster over 7 runs: raw `7.198 ms [5.636, 8.939]` vs SQLite `76.980 ms [73.539, 102.457]` with `db_ok=7/7`, and coltx `7.781 ms [7.470, 8.873]` vs SQLite `76.980 ms [73.539, 102.457]` with `db_ok=7/7`. The proof target also printed protected benchmark artifacts clean before and after.
- On 2026-07-07, `tests/crash_recover_smoke.sh` was extended to cover corrupt dictionary artifact fallback through the installed public path. The smoke now builds and runs the existing `benchmark_test/db_tx_dict_smoke.sa` against the dev-installed plugin to create a dictionary-backed committed epoch, corrupts the latest dictionary data file, verifies `sa db verify` fails, and then checks `sa db recover` republishes the previous valid zero-row epoch. `build.zig`'s `crash-recover-smoke` description now explicitly lists corrupt meta/segment/index/dict artifacts. Verified with `zig build crash-recover-smoke`.
- On 2026-07-07, `build.zig` added `zig build sqlite-proof` as the broadest current proof entry. It depends on `sqlite-audit` for non-benchmark readiness evidence and on `bench-compare-proof` for the 7-run disk/memory/concurrent SQLite performance proof, then prints a final pass summary that explicitly keeps disk combined append chain report-only unless strict-chain is requested. Verified with `zig build sqlite-proof`: disk total append chain reported db `4.284 ms [4.180, 5.337]` vs SQLite `6.025 ms [3.962, 6.320]`, memory total append chain db `2.790 ms [2.443, 3.390]` vs SQLite `4.020 ms [3.186, 6.283]`, best concurrent query db `4.680 ms [4.051, 9.272]` vs SQLite `126.096 ms [117.374, 141.160]`, raw concurrent insert db `6.267 ms [5.254, 8.152]` vs SQLite `82.769 ms [79.285, 104.569]` with `db_ok=7/7`, and coltx concurrent insert db `8.102 ms [7.616, 11.155]` vs SQLite `82.769 ms [79.285, 104.569]` with `db_ok=7/7`. The command also ran `sqlite-audit` and ended with protected benchmark artifacts clean.
- On 2026-07-07, `build.zig` added opt-in strict-chain proof targets: `zig build bench-compare-proof-strict-chain` and `zig build sqlite-proof-strict-chain`. They reuse the proof pipeline but set disk and memory combined append chain to hard gates while keeping strict concurrent insert. These targets are for collecting repeated upgrade evidence only; default `bench-compare-proof` and `sqlite-proof` remain conservative because disk combined append has shown variance across proof samples. A wiring check with `zig build sqlite-proof-strict-chain -Dbench-compare-proof-runs=1` correctly failed the disk combined append hard gate (`db=4.964 ms`, SQLite `3.992 ms`), which is negative evidence against making strict-chain default yet.
- A follow-up default `zig build sqlite-proof` still passed after adding the strict-chain targets and produced another variance sample: disk total append chain was only report-only `WARN` at db `4.038 ms [3.546, 4.487]` vs SQLite `3.989 ms [3.519, 5.131]`, while memory total append chain passed at db `2.204 ms [2.138, 3.252]` vs SQLite `3.507 ms [3.225, 4.643]`. Strict concurrent insert remained stable over 7 runs: raw `5.912 ms [5.502, 8.980]` vs SQLite `99.104 ms [77.259, 106.191]` with `db_ok=7/7`, and coltx `8.757 ms [7.452, 11.543]` vs SQLite `99.104 ms [77.259, 106.191]` with `db_ok=7/7`. This reinforces keeping disk combined append report-only by default.
- On 2026-07-07, `build.zig` added narrower opt-in strict-chain targets: `zig build bench-compare-disk-strict-chain` and `zig build bench-compare-memory-strict-chain`. They run only the disk or memory indexed ERP strict combined append-chain gate, respectively, using `-Dbench-compare-proof-runs` for run count and protected artifact guards before/after. These targets make it cheaper to collect targeted disk variance evidence or confirm memory-chain stability without rerunning the concurrent proof every time. Verified the success path with `zig build bench-compare-memory-strict-chain -Dbench-compare-proof-runs=1`: memory total append chain `2.377 ms` vs SQLite `4.848 ms`, and protected benchmark artifacts were clean before and after.
- On 2026-07-07, `build.zig` added `zig build bench-compare-disk` as the disk-only counterpart to `bench-compare-memory`. It runs just the disk indexed ERP db-vs-SQLite median gate with protected artifact guards before and after, using `-Dbench-compare-runs` and the existing opt-in `-Dbench-compare-strict-chain=true` switch. This gives a cheap durable-path diagnostic entry without rerunning memory or concurrent proof stages. A 1-run diagnostic (`zig build bench-compare-disk -Dbench-compare-runs=1`) correctly exposed variance by failing the hard `tx append` gate (`db=3.764 ms`, SQLite `3.612 ms`). The default 3-run median gate passed: disk total append chain reported db `4.106 ms [3.701, 4.468]` vs SQLite `4.272 ms [4.070, 5.718]`, only `0.166 ms` ahead, so combined append remains too close for default strict-chain.
- On 2026-07-07, `build.zig` added `zig build check-docs` and made `zig build test` depend on it. The guard now dynamically parses docs-relevant targets from `build.zig` (`sqlite-*`, `bench-compare*`, `check-docs`, `check-proof-wiring`, and `check-benchmark*`) and requires `tasks.md` and `progress.md` to stay non-empty and mention them, plus the disk combined append report-only caveat. This is not proof of full SQLite parity; it prevents the proof/status documentation from silently drifting behind the build targets. The `sqlite-audit` pass summary now explicitly says it ran the docs guard and benchmark parser guards, and `check-docs` verifies that summary text so audit output cannot silently omit these default gates.
- On 2026-07-07, `zig build test` was tightened to depend directly on `check-benchmark-artifacts`, not only through `sqlite-audit` or compare targets. This means the default test gate now fails if protected tracked benchmark artifacts (`benchmark_test/.bench_coltx`, `.bench_raw`, `.bench_sqlite`, or `sqlite_erp_indexed_write.db`) have a git diff, preventing ordinary test runs from reporting green while benchmark evidence is dirty.
- On 2026-07-07, `build.zig` made `zig build sqlite-audit` depend on `zig build bench`. The non-benchmark audit now verifies that both db and SQLite control benchmark executable builds still compile, in addition to the default test/docs/benchmark-parser/ABI/facade/layout/install/recovery/public-smoke gates and protected artifact guard. `tests/check_docs.sh` now also requires this audit dependency and summary text so the benchmark-build evidence and benchmark parser guards cannot silently disappear from `sqlite-audit`, and it locks the `sqlite-proof` / `sqlite-proof-strict-chain` proof wiring so broad proof entry points continue to run `sqlite-audit` plus their matching proof artifact chains. This does not execute benchmark comparisons from `sqlite-audit`, does not change the disk combined append report-only policy, and is not proof of full SQLite parity; performance evidence still comes from `bench-compare-proof` or `sqlite-proof`.
- On 2026-07-07, `build.zig` added `zig build check-proof-wiring` and made `zig build test` depend on it. The new guard checks the `sqlite-audit`, `bench-compare-proof`, `bench-compare-proof-strict-chain`, `sqlite-proof`, and `sqlite-proof-strict-chain` dependency chains directly in `build.zig`, including benchmark executable builds, protected artifact guards, non-strict report-only proof wiring, and strict-chain proof wiring. `check-docs` now treats `check-proof-wiring` as docs-relevant, so `tasks.md` and `progress.md` must mention the target. This is not proof of full SQLite parity and does not execute benchmark comparisons; it prevents broad proof targets from silently losing required prerequisite gates.
- On 2026-07-07, the `sqlite-audit` final summary was aligned with the actual gate set by explicitly listing benchmark parser guards and `proof wiring` alongside benchmark executable builds and protected benchmark artifact cleanliness. `tests/check_proof_wiring.sh` now checks that summary text, so audit output cannot silently omit the parser/proof wiring guards while the dependencies still run. This is not proof of full SQLite parity and does not execute benchmark comparisons; it makes the non-benchmark audit output match the readiness gates it actually ran.
- On 2026-07-07, a fresh `zig build sqlite-proof` run passed the broad default proof after the proof-wiring/audit summary updates. Disk indexed ERP hard stages passed, while the total append chain remained report-only `WARN`: db `4.241 ms [3.975, 4.634]` vs SQLite `4.169 ms [3.649, 5.888]`, so disk combined append still should not become a default hard gate. Memory total append chain passed at db `2.417 ms [2.152, 2.765]` vs SQLite `4.398 ms [3.114, 5.590]`. Concurrent strict insert stayed correct and faster over 7 runs: raw `6.165 ms [5.559, 6.835]` vs SQLite `76.352 ms [71.424, 105.355]` with `db_ok=7/7`, and coltx `7.493 ms [6.868, 9.920]` vs SQLite `76.352 ms [71.424, 105.355]` with `db_ok=7/7`. The command ran `sqlite-audit`, printed proof wiring and protected benchmark artifact cleanliness, and is not proof of full SQLite parity.
- On 2026-07-07, `zig build bench-compare-disk-strict-chain -Dbench-compare-proof-runs=7` passed as opt-in disk strict-chain evidence: init `0.176 ms [0.165, 0.536]` vs SQLite `0.778 ms [0.679, 0.898]`, baseline `4.442 ms [4.207, 5.133]` vs `62.465 ms [60.214, 70.762]`, build `8.606 ms [8.220, 8.826]` vs `19.739 ms [18.229, 22.760]`, tx append `2.317 ms [2.164, 2.722]` vs `4.007 ms [3.883, 4.771]`, coltx append `1.546 ms [1.472, 1.687]` vs `4.007 ms [3.883, 4.771]`, verify `0.869 ms [0.813, 0.959]` vs `34.791 ms [32.683, 37.901]`, and total append chain `3.946 ms [3.711, 4.195]` vs SQLite `4.007 ms [3.883, 4.771]` with only `0.061 ms` median margin. This is opt-in positive evidence but, combined with the latest `sqlite-proof` disk-chain `WARN`, does not justify making disk combined append a default hard gate.
- On 2026-07-07, `build.zig` hardened both benchmark compare Python runners with explicit metric validation. Indexed disk/memory comparisons now require all db and SQLite timing metrics plus the three row-count metrics before row-count parity, medians, margins, or strict-chain checks run; concurrent comparisons now require serial/concurrent query correctness/timing and concurrent insert correctness/row-count/timing metrics for raw db, coltx db, and SQLite samples before any correctness or performance interpretation. Missing required metrics now fail with sample-specific `missing metrics` diagnostics instead of ambiguous Python key errors, any non-integer `key=value` metric fails immediately with `benchmark output invalid integer metric`, negative metrics fail immediately with `benchmark output negative metric`, concurrent `*_ok` status metrics must be `0` or `1`, duplicate metric names fail instead of letting later output silently overwrite earlier values, and timing `*_ns` metrics must be positive so zero-duration samples cannot enter medians or margins. This improves benchmark evidence integrity only; it is not proof of full SQLite parity and does not change the disk combined append report-only or strict-chain opt-in policy.
- On 2026-07-07, `build.zig` added `zig build check-benchmark-parser-guards` and `tests/check_benchmark_parser_guards.sh`, then wired the target into `zig build test`. The guard checks that both benchmark compare parsers still fail on duplicate metric names, fail immediately on invalid-integer or negative metric values, reject non-positive timing metrics, reject non-boolean concurrent status metrics, emit missing-metric diagnostics for required keys, and keep the critical indexed append and concurrent insert metric keys in the required lists. `tests/check_docs.sh` now treats `check-benchmark-parser-guards` as docs-relevant, and `tests/check_proof_wiring.sh` verifies that `sqlite-audit` output mentions benchmark parser guards. This is not proof of full SQLite parity and does not execute benchmark comparisons; it prevents future build wiring edits from weakening benchmark evidence integrity unnoticed.
- On 2026-07-07, the concurrent benchmark parser guard was tightened again so concurrent insert status and row-count metrics cannot contradict each other before report or strict-insert interpretation. Raw db, coltx db, and SQLite samples now fail if reported inserted rows exceed `50000`, if `ok=1` reports anything other than the full `50000` rows, or if `ok=0` still claims the full `50000` rows. `tests/check_benchmark_parser_guards.sh` locks the helper and call sites. This rejects inconsistent benchmark output only; it does not make default concurrent insert strict, does not change the disk combined append report-only policy, and is not proof of full SQLite parity.
- On 2026-07-07, `build.zig` added the explicit narrow proof target `zig build bench-compare-concurrent-strict-insert`. It runs only the concurrent db-vs-SQLite comparison with strict concurrent insert correctness/performance enabled, uses `-Dbench-compare-proof-runs` for sample count, and keeps the same dev-install, benchmark-run lock, and protected artifact guards before and after execution. `tests/check_proof_wiring.sh` now locks that wiring. This is an opt-in evidence collector for graduating concurrent insert strictness later; it does not change default `bench-compare`, does not make disk combined append strict, and is not proof of full SQLite parity.
- On 2026-07-07, the benchmark compare parser guard now also locks explicit non-positive run-count rejection in both indexed disk/memory and concurrent comparison runners. Both Python runners fail with `benchmark compare run count must be positive` before sampling when `runs <= 0`, which prevents empty sample sets from reaching median, margin, strict-chain, or strict-insert interpretation. `tests/check_benchmark_parser_guards.sh` now requires that guard in both runners. This improves benchmark evidence integrity only; it is not proof of full SQLite parity and does not change any default strictness policy.
- On 2026-07-07, `build.zig` tightened the concurrent query performance gate so raw and coltx paths are each compared directly against SQLite for both serial and concurrent SUM queries. The gate no longer chooses the faster db path as `best serial query` / `best concurrent query`, so a regression in either raw `sa_db_ingest_columns` roots or coltx roots cannot be hidden by the other path. `tests/check_benchmark_parser_guards.sh` now requires the four per-path `print_check` calls and forbids the previous best-path checks. This strengthens query evidence without changing concurrent insert strictness, disk combined append report-only policy, or any full SQLite parity caveat.
- On 2026-07-07, `build.zig` added a final success summary to `zig build bench-compare-concurrent-strict-insert`: `bench-compare-concurrent-strict-insert passed: strict raw/coltx concurrent insert proof and protected artifact guard completed`. The summary depends on the post-run protected benchmark artifact guard, so the target cannot print its final pass line before both strict raw/coltx insert proof and artifact cleanliness complete. `tests/check_proof_wiring.sh` now locks that dependency chain. This is only proof-output and wiring integrity; it does not change default concurrent insert strictness, disk combined append report-only policy, or any full SQLite parity caveat.
- On 2026-07-07, both benchmark compare Python runners now reject malformed strictness flags before any benchmark interpretation. The indexed disk/memory runner requires `strict_chain_s` to be exactly `"0"` or `"1"` and fails with `benchmark compare strict-chain flag must be 0 or 1`; the concurrent runner does the same for `strict_insert_s` with `benchmark compare strict-insert flag must be 0 or 1`. `tests/check_benchmark_parser_guards.sh` locks both checks. This prevents accidental proof wiring or script changes from silently downgrading strict gates to report-only behavior; it does not change the default report-only/opt-in strict policies and is not proof of full SQLite parity.
- On 2026-07-07, `build.zig` proof target descriptions and the `sqlite-proof` final summary were aligned with the configurable proof run count. `bench-compare-proof`, `bench-compare-proof-strict-chain`, `sqlite-proof`, and `sqlite-proof-strict-chain` now describe configured-run proof behavior with default `7` instead of implying every invocation is fixed at `7` runs, and the `sqlite-proof` pass line now includes the configured proof sample count as `runs=N`. `tests/check_docs.sh` and `tests/check_proof_wiring.sh` were updated to lock the run-count wording. This is proof-output accuracy only; historical 7-run evidence remains historical 7-run evidence, default strictness policies are unchanged, and this is not proof of full SQLite parity.
- A follow-up `zig build sqlite-proof -Dbench-compare-proof-runs=1` was used only as a configurable-run wording/wiring diagnostic and did not pass: the single disk indexed ERP `init` hard gate measured db `0.852 ms` vs SQLite `0.797 ms`. This is recorded as 1-run variance/negative diagnostic evidence and is not a failed docs/proof-wiring check, not a 7-run proof result, and not a reason to promote or demote the existing default strictness policies by itself.
- On 2026-07-07, `build.zig` added final pass summaries for `zig build bench-compare-proof` and `zig build bench-compare-proof-strict-chain`. The non-strict proof now prints `bench-compare-proof passed: runs=N disk/memory proof report plus strict concurrent insert and protected artifact guard completed` only after the post-run protected artifact guard, and the strict-chain proof now prints `bench-compare-proof-strict-chain passed: runs=N strict disk/memory combined append proof plus strict concurrent insert and protected artifact guard completed` after its matching artifact guard. `sqlite-proof` and `sqlite-proof-strict-chain` now depend on those proof summaries instead of the raw artifact steps, and `tests/check_docs.sh` / `tests/check_proof_wiring.sh` lock the `runs={d}` source strings and dependency chain. This is proof-output and wiring integrity only; default strictness policies are unchanged, disk combined append remains report-only unless strict-chain is requested, and this is not proof of full SQLite parity.
- On 2026-07-07, the narrower strict-chain targets now also have final pass summaries after their post-run artifact guards. `bench-compare-disk-strict-chain` prints `bench-compare-disk-strict-chain passed: runs=N disk strict combined append proof and protected artifact guard completed`, and `bench-compare-memory-strict-chain` prints `bench-compare-memory-strict-chain passed: runs=N memory strict combined append proof and protected artifact guard completed`. `tests/check_docs.sh` and `tests/check_proof_wiring.sh` lock both `runs={d}` summary strings and dependency chains. This improves proof-output consistency only; disk strict-chain remains opt-in and can still fail on variance.
- On 2026-07-07, the public compare targets now also print final summaries after their post-run protected artifact guards. `bench-compare`, `bench-compare-disk`, `bench-compare-memory`, and `bench-compare-concurrent` each end with a `passed: runs=N ... protected artifact guard completed` line only after the matching artifact guard step succeeds. `tests/check_docs.sh` and `tests/check_proof_wiring.sh` lock these `runs={d}` source strings and dependencies. This is output/wiring integrity only; it does not change disk combined append report-only behavior, strict-chain opt-in behavior, or any SQLite parity caveat.
- On 2026-07-07, the compare/proof final summaries were tightened again so every public `bench-compare*`, `bench-compare-*-strict-*`, `sqlite-proof`, and `sqlite-proof-strict-chain` pass line prints the actual configured run count as `runs=N` instead of only saying configured-run behavior. This makes 1-run diagnostics and default 3/7-run proof output distinguishable in logs while keeping the same dependencies, artifact guards, strict-chain opt-in policy, concurrent-insert opt-in policy, and SQLite parity caveats.
- On 2026-07-07, `tests/check_docs.sh` now also validates `benchmark_test/RESULTS.md` so benchmark evidence docs cannot drift back to describing `bench-compare-proof` or `sqlite-proof` as fixed 7-run-only targets. The guard requires configured-run wording, default `7`, final `runs=N` summaries, report-only/strict-chain caveats, and the broader SQLite parity caveat, and rejects the stale phrase `固定运行 7 轮`. `build.zig` lists `benchmark_test/RESULTS.md` as a docs-guard input, and `tasks.md` records the expanded docs contract. This is documentation/evidence integrity only; it does not execute benchmark comparisons, change default strictness, or prove full SQLite parity.
- On 2026-07-07, `benchmark_test/RESULTS.md` was also aligned with the current per-path concurrent query gate. The current proof/result tables now describe `raw serial query`, `coltx serial query`, `raw concurrent query`, and `coltx concurrent query` instead of stale `best serial query` / `best concurrent query` summaries, and `tests/check_docs.sh` rejects reintroducing those best-path labels in benchmark evidence docs. This protects against one fast db path hiding a regression in the other documented path; it is evidence-documentation integrity only and does not change default strictness or prove full SQLite parity.
- On 2026-07-06, `build.zig` expanded `zig build abi-smoke` from the single lifecycle smoke to the full public `benchmark_test/db_*_smoke.sa` suite. The target now builds and runs 43 SA-facing `db.sal` smokes from temporary cwd roots against the isolated dev-installed plugin, covering blob stores/indexes/filters/planners, bool/float/small numeric/timestamp typed queries, grouped sum/stats, candidate filters and compacted candidate sort/filter flows, typed key reads and writes, transaction/blob/dict flows, memory roots, lifecycle restore/recover, date/decimal planners, and interface projection/sum behavior. This turns the previously documented manual smoke evidence into a default `zig build test` gate. Verified with a manual all-smoke loop, `zig build abi-smoke`, `zig build test`, and `zig build`.
- On 2026-07-06, `build.zig` added `zig build check-abi-symbols` and `tests/check_abi_symbols.sh`. The gate parses every `sa_db_*` extern declared in `db.sai`, compares it with `nm -D --defined-only` output from the emitted `libdb.so`, and fails on either missing exports or undeclared `sa_db_*` native exports. The current gate reports `db ABI symbols match: declared=306 exported_sa_db=306`, and `zig build test` now depends on it so the public ABI cannot silently drift from the native library. Verified with `zig build check-abi-symbols`, `zig build test`, and `zig build`.
- On 2026-07-06, `build.zig` added `zig build check-sal-facade` and `tests/check_sal_facade.sh`. The gate compares the `sa_db_*` extern set declared in `db.sai` with the `@sa_db_*` calls referenced by `db.sal`, failing on missing facade coverage or undeclared native calls. The current gate reports `db.sal facade covers db.sai: declared=306 facade_calls=306`, and `zig build test` now depends on it so SA-facing macro coverage stays synchronized with the native ABI. Verified with `zig build check-sal-facade`, `zig build check-abi-symbols`, `zig build test`, and `zig build`.
- On 2026-07-06, `build.zig` added `zig build check-manifest-layout` and `tests/check_manifest_layout.sh`, and `install-smoke` now reruns that script against the dev-installed `current` directory. The gate verifies `sap.json` identity (`db`/`0.1.0`), local source, ABI symbol file, Linux `libdb.so` artifact path, `db.sai`/`db.sal` interface paths, `database` skill, empty dependencies, no net/env/process-spawn permissions, the four `$PROJECT/**` fs permissions, and installed `current/{libdb.so,sap.json,sa/db.sai,sa/db.sal,permissions.lock}` layout with an exact manifest copy. Verified with `zig build check-manifest-layout`, `zig build install-smoke`, `zig build test`, and `zig build`.
- On 2026-07-06, `build.zig` added `zig build check-benchmark-artifacts` and `tests/check_benchmark_artifacts.sh`. The guard checks that protected tracked benchmark artifacts (`benchmark_test/.bench_coltx`, `.bench_raw`, `.bench_sqlite`, and `sqlite_erp_indexed_write.db`) have no git diff, and `bench-compare`, `bench-compare-concurrent`, and `bench-compare-proof` now run it after their comparison scripts. Verified with `zig build check-benchmark-artifacts`, `zig build test`, `zig build`, and `zig build bench-compare-concurrent`; the concurrent comparison passed with best serial query `4.380 ms` vs SQLite `307.797 ms`, best concurrent query `4.240 ms` vs SQLite `98.377 ms`, raw insert `6.210 ms` with `db_ok=3/3`, coltx insert `7.380 ms` with `db_ok=3/3`, and the final artifact guard reported clean.
- On 2026-07-06, benchmark artifact guarding was tightened again. `bench-compare`, `bench-compare-memory`, `bench-compare-concurrent`, and `bench-compare-proof` now start from a clean protected-artifact guard, and `bench-compare-memory` also has a post-compare guard. Verified with `zig build check-benchmark-artifacts`, `zig build bench-compare-memory`, `zig build test`, and `zig build`. The memory compare printed guard-clean before and after, and passed all SQLite gates with init `0.310 ms` vs SQLite `0.598 ms`, baseline `2.887 ms` vs `64.691 ms`, build `7.681 ms` vs `20.508 ms`, tx append `1.525 ms` vs `3.803 ms`, coltx append `0.985 ms` vs `3.803 ms`, verify `0.230 ms` vs `34.478 ms`, and total append chain `2.511 ms` vs `3.803 ms`.
- On 2026-07-06, `build.zig` added `zig build check-abi-smoke-coverage` and `tests/check_abi_smoke_coverage.sh`. The gate parses `abi_smoke_sources` in `build.zig` and compares it with the filesystem set of `benchmark_test/db_*_smoke.sa`, failing on missing or stale list entries. The current gate reports `abi smoke coverage complete: files=43 listed=43`, and both `abi-smoke` and `zig build test` depend on it so new public ABI smokes cannot be forgotten. Verified with `zig build check-abi-smoke-coverage`, `zig build abi-smoke`, `zig build test`, and `zig build`.
- On 2026-07-06, `install-smoke` added `tests/check_cli_surface.sh`. The script parses the `database` skill item list from `src/plugin.zig`, reads the installed plugin's `sa skills` text output from the isolated dev-install home, and fails unless the two command surfaces match exactly. The current installed CLI surface reports `database CLI skill surface matches: items=14`. Verified with `zig build install-smoke`, `zig build test`, and `zig build`.
- On 2026-07-06, `tests/check_cli_surface.sh` was strengthened to validate the installed plugin's machine-readable `sa skills --json` output in addition to the text output. The JSON parser requires a top-level `skills` array, a `database` section, and a string `items` array matching `src/plugin.zig` exactly. The current installed CLI surface reports `database CLI skill surface matches text/json: items=14`. Verified with `bash -n tests/check_cli_surface.sh`, `SA_PLUGIN_DEV=1 SA_PLUGINS_HOME=.zig-cache/db-install-smoke-home bash tests/check_cli_surface.sh /home/vscode/projects/sci/zig-out/bin/sa src/plugin.zig`, `zig build install-smoke`, `zig build test`, and `zig build`.
- On 2026-07-06, `build.zig` added `zig build sqlite-audit` as a single non-benchmark SQLite-readiness audit target. It depends on the default `zig build test` gate set and `check-benchmark-artifacts`, then prints a concise summary covering Zig tests, ABI symbol/facade checks, manifest/install layout, installed CLI text/json surface, crash recovery smokes, ABI smoke coverage, public ABI smokes, and protected benchmark artifact cleanliness. Verified with `zig build sqlite-audit`, `zig build test`, and `zig build`; `sqlite-audit` reported protected artifacts clean and printed `sqlite-audit passed: zig tests, ABI/facade/layout, install+CLI/recovery smokes, ABI smoke coverage, public ABI smokes, and protected benchmark artifacts are clean`.
- On 2026-07-06, `tests/check_manifest_layout.sh` was strengthened so the installed-layout check now compares installed `current/sa/db.sai` and `current/sa/db.sal` byte-for-byte with source `db.sai` and `db.sal`, not just their existence. `build.zig` passes the source interface files to the installed-layout invocation inside `install-smoke`, preventing a dev-installed package from carrying stale public interfaces while source-level checks still pass. Verified with `zig build check-manifest-layout`, `zig build install-smoke`, `zig build test`, and `zig build`.
- On 2026-07-06, the installed-layout check was extended to compare installed `current/libdb.so` byte-for-byte with the manifest artifact `zig-out/lib/libdb.so`. The first attempt compared against Zig's internal `lib.getEmittedBin()` cache path and failed, which confirmed that dev install follows `sap.json`'s artifact path rather than the internal cache artifact. `build.zig` now passes `zig-out/lib/libdb.so` to `tests/check_manifest_layout.sh`, so stale installed native libraries are caught by `install-smoke`. Verified with `zig build install-smoke`, `zig build test`, and `zig build`.
- On 2026-07-06, `tests/check_manifest_layout.sh` was strengthened to validate installed `permissions.lock` content, not only its presence. It now checks `schema=sa.permissions/1`, `plugin=db`, `version=0.1.0`, `permissions_sha256` computed as `jq -c '.permissions' sap.json | tr -d '\n' | sha256sum`, the empty dependency graph hash, `requires_confirmation=true`, `confirmed=false`, `artifact_scan=dynamic-imports`, and `sandbox_enforced=false`. Verified with `zig build install-smoke`, `zig build test`, and `zig build`.
- `zig build test` passes in `/home/vscode/projects/sa_plugins/sa_plugin_db`.
- On 2026-06-17, `zig test src/table.zig` and `zig test src/db_saasm_api.zig` both pass after fixing lazy-append write-transaction materialization to rebuild full-row buffers from the committed `base_row_count` snapshot plus `pending_append_buffers`, which stabilizes recovery of incomplete transaction metadata before index rebuild.
- On 2026-06-17, append-only raw ingest, `coltx` commit, and pure append write-transaction commit now maintain existing single-column, `u64_pair`, `u64_i64_pair`, `blob_eq`, `blob_token`, `blob_prefix`, and `blob_contains` indexes incrementally by merging prior index bytes with the new segment's sorted entries, with automatic fallback to full `rebuildIndexes()` for unsupported/non-append paths. Verified by rerunning full `zig test src/table.zig` and `zig test src/db_saasm_api.zig`, plus targeted assertions that appended `u64` keys remain findable after transaction and `coltx` commits.
- On 2026-06-17, `benchmark_test/db_erp_indexed_write_bench.out` and the SQLite control `benchmark_test/sqlite_erp_indexed_write_bench.out` both pass with matching appended row totals (`8448 / 33792 / 8448`). The db plugin reports `db_erp_indexed_build_ns=129138857`, `db_erp_indexed_tx_append_ns=53741938`, and `db_erp_indexed_coltx_append_ns=52369384`; the SQLite control reports `sqlite_erp_indexed_build_ns=27890122` and `sqlite_erp_indexed_append_ns=5062835`. Relative to the earlier ERP workflow median (`build ERP indexes = 175.708 ms`), the db indexed-build stage is down about `26.5%`, and append-only indexed ERP writes are now measured directly instead of paying a full post-append rebuild.
- On 2026-06-18, incremental append index maintenance now reads the existing index artifact through `mmap` during merge instead of heap-copying it with `readFileAlloc`. Full `zig test src/table.zig` and `zig test src/db_saasm_api.zig` still pass, and the isolated concurrent reruns kept the db plugin ahead of SQLite on serial and concurrent SUM plus both raw and `coltx` concurrent insert paths. The latest 3-run medians were about `db_serial_query_ns=167316960`, `db_concurrent_query_ns=157531433`, `db_concurrent_insert_ns=95736479`, `db_coltx_concurrent_insert_ns=72191503`, and the SQLite control medians were about `sqlite_serial_query_ns=338595317`, `sqlite_concurrent_query_ns=197866481`, `sqlite_concurrent_insert_ns=108089037`.
- On 2026-06-18, index creation for a single table can now be batched through `sa_db_create_indexes` / `table.createIndexes`, which removed repeated lock/load/meta/manifest overhead for the two-index `lines` and `invoices` tables. The current benchmark gap now sits mostly in the durable write append sub-stages, not in the read-side or no-sync `:memory:` path.
- `zig build` passes and produces `zig-out/lib/libdb.so` for the `linux-x86_64` artifact path in `sap.json`.
- `sa build-exe benchmark_test/db_group_sum_smoke.sa -o benchmark_test/db_group_sum_smoke.out --no-incremental` plus the resulting smoke binary pass and cover `DB_GROUP_SUM_I64_BY_U64_HANDLE`, `DB_GROUP_ROWS_SUM_I64_BY_U64_HANDLE`, `DB_GROUP_SUM_I64_BY_U64_SORTED_HANDLE`, and `DB_GROUP_ROWS_SUM_I64_BY_U64_SORTED_HANDLE` for full-table grouping, paged grouping, sorted full-table Top N grouping, candidate-row grouping, sorted candidate-row grouping, and empty candidate grouping through `db.sal`.
- `sa build-exe benchmark_test/db_group_stats_smoke.sa -o benchmark_test/db_group_stats_smoke.out --no-incremental` plus the resulting smoke binary pass and cover `DB_GROUP_STATS_I64_BY_U64_HANDLE`, `DB_GROUP_ROWS_STATS_I64_BY_U64_HANDLE`, `DB_GROUP_STATS_I64_BY_U64_SORTED_HANDLE`, and `DB_GROUP_ROWS_STATS_I64_BY_U64_SORTED_HANDLE` for full-table grouped `count/sum/min/max`, sorted grouped `min/max` pages, candidate-row grouped stats, sorted candidate-row grouped stats, and empty candidate grouping through `db.sal`.
- `sa build-exe benchmark_test/db_interface_smoke.sa -o benchmark_test/db_interface_smoke.out --no-incremental` plus the resulting smoke binary pass and cover `DB_PROJECT_ROWS_HANDLE` for both populated projected pages and empty projected pages (`written_rows=0`, `required_bytes=0`) plus `DB_SUM_I64_HANDLE` signed balance aggregation through `db.sal`.
- `sa build-exe benchmark_test/db_update_row_smoke.sa -o benchmark_test/db_update_row_smoke.out --no-incremental` plus the resulting smoke binary pass and cover strict row update, transaction row update, and missing-key `SA_DB_ERR_NOT_FOUND` behavior through `db.sal`.
- `sa build-exe benchmark_test/db_i64_key_write_smoke.sa -o benchmark_test/db_i64_key_write_smoke.out --no-incremental` plus the resulting smoke binary pass and cover direct and transaction upsert/update/delete by unique signed `i64` key through `db.sal`, including negative keys and rollback invisibility.
- `sa build-exe benchmark_test/db_u32_i32_key_write_smoke.sa -o benchmark_test/db_u32_i32_key_write_smoke.out --no-incremental` plus the resulting smoke binary pass and cover direct and transaction upsert/update/delete by unique compact `u32` and `i32` keys through `db.sal`, including signed compact keys and rollback invisibility.
- `sa build-exe benchmark_test/db_u8_i8_u16_i16_key_write_smoke.sa -o benchmark_test/db_u8_i8_u16_i16_key_write_smoke.out --no-incremental` plus the resulting smoke binary pass and cover all direct and transaction upsert/update/delete macros for unique compact `u8`, `i8`, `u16`, and `i16` keys through `db.sal`, including rollback invisibility.
- `sa build-exe benchmark_test/db_typed_key_get_row_smoke.sa -o benchmark_test/db_typed_key_get_row_smoke.out --no-incremental` plus the resulting smoke binary pass and cover full fixed-width row reads by unique `i64`, `u32`, `i32`, `u8`, `i8`, `u16`, and `i16` keys through `db.sal`.
- `sa build-exe benchmark_test/db_pair_key_get_row_smoke.sa -o benchmark_test/db_pair_key_get_row_smoke.out --no-incremental` plus the resulting smoke binary pass and cover full fixed-width row reads by unique `(u64, u64)` and `(u64, i64)` keys through `db.sal`.
- `sa build-exe benchmark_test/db_blob_key_get_row_smoke.sa -o benchmark_test/db_blob_key_get_row_smoke.out --no-incremental` plus the resulting smoke binary pass and cover full fixed-width row reads by unique `blob_eq` text/blob keys through `db.sal`.
- `sa build-exe benchmark_test/db_blob_key_write_smoke.sa -o benchmark_test/db_blob_key_write_smoke.out --no-incremental` plus the resulting smoke binary pass and cover direct and transaction upsert/update/delete by unique `blob_eq` text/blob keys through `db.sal`, including exact row-handle validation and rollback invisibility.
- `sa build-exe benchmark_test/db_u64_pair_write_smoke.sa -o benchmark_test/db_u64_pair_write_smoke.out --no-incremental` plus the resulting smoke binary pass and cover direct and transaction upsert/update/delete by unique `(u64, u64)` key through `db.sal`.
- `sa build-exe benchmark_test/db_u64_i64_pair_write_smoke.sa -o benchmark_test/db_u64_i64_pair_write_smoke.out --no-incremental` plus the resulting smoke binary pass and cover direct and transaction upsert/update/delete by unique `(u64, i64)` key through `db.sal`, including negative signed second keys and rollback invisibility.
- `sa build-exe benchmark_test/db_tx_dict_smoke.sa -o benchmark_test/db_tx_dict_smoke.out --no-incremental` plus the resulting smoke binary pass and cover transaction dictionary intern, commit visibility, row references, and rollback invisibility through `db.sal`.
- `sa build-exe benchmark_test/db_candidate_filter_smoke.sa -o benchmark_test/db_candidate_filter_smoke.out --no-incremental` plus the resulting smoke binary pass and cover candidate row filters, nullable candidate-row `u64/i64` range filters driven by sidecar null bitmaps, intersection, union, exclusion, stats, and sorting through `db.sal`.
- `sa build-exe benchmark_test/db_planner_smoke.sa -o benchmark_test/db_planner_smoke.out --no-incremental` plus the resulting smoke binary pass and cover `DB_PLAN_U64_I64_RANGES_HANDLE`, `DB_PLAN_U64_U64_RANGES_HANDLE`, `DB_PLAN_I64_I64_RANGES_HANDLE`, `SaDbPlanInfo`, smaller-side choice, and pagination through `db.sal`.
- `sa build-exe benchmark_test/db_blob_planner_smoke.sa -o benchmark_test/db_blob_planner_smoke.out --no-incremental` plus the resulting smoke binary pass and cover `DB_PLAN_U64_BLOB_EQ_HANDLE`, `DB_PLAN_I64_BLOB_EQ_HANDLE`, `DB_PLAN_U64_I64_BLOB_EQ_HANDLE`, `SaDbPlanInfo`, `SaDbPlan3Info`, smaller-side choice, range-first/blob-first plans, three-predicate planning, and pagination through `db.sal`.
- `sa build-exe benchmark_test/db_bool_planner_smoke.sa -o benchmark_test/db_bool_planner_smoke.out --no-incremental` plus the resulting smoke binary pass and cover `DB_PLAN_U64_I64_BOOL_HANDLE`, `SaDbPlan3Info`, i64-first/bool-first/u64-first plans, three-predicate planning, and pagination through `db.sal`.
- `sa build-exe benchmark_test/db_i64_i64_planner_smoke.sa -o benchmark_test/db_i64_i64_planner_smoke.out --no-incremental` plus the resulting smoke binary pass and cover `DB_PLAN_U64_I64_I64_RANGES_HANDLE`, `SaDbPlan3Info`, amount-first/date-first/u64-first plans, three-predicate planning, and pagination through `db.sal`.
- `sa build-exe benchmark_test/db_date_decimal_planner_smoke.sa -o benchmark_test/db_date_decimal_planner_smoke.out --no-incremental` plus the resulting smoke binary pass and cover `DB_PLAN_U64_DATE_DECIMAL_I64_HANDLE`, `SaDbPlan3Info`, semantic date/decimal boundary conversion, amount-first/date-first plans, and invalid-date rejection through `db.sal`.
- `sa build-exe` plus execution pass for `db_blob_filter_smoke.sa`, `db_blob_token_smoke.sa`, and `db_blob_prefix_smoke.sa`, covering candidate row text/blob exact, contains, token, and prefix filters through `db.sal`.
- `bash tests/smoke_installed.sh` passes and verifies the dev-installed plugin command path, including qmod register/inspect/exec and a bitwise DB filter qmod.
- `nm -D zig-out/lib/libdb.so | rg 'sa_db_group_(rows_)?sum_i64_by_u64(_sorted)?_handle$|sa_db_sum_(u64|i64)_handle$|sa_db_plan_(u64_i64|u64_u64|i64_i64)_ranges_handle$|sa_db_plan_(u64|i64)_blob_eq_handle$|sa_db_plan_u64_i64_(blob_eq|bool|i64_ranges)_handle$|sa_db_plan_u64_date_decimal_i64_handle$|sa_db_filter_rows_(u64|i64|decimal_i64|date|timestamp_ms|timestamp_us)_range_null_bitmap_handle$'` verifies the group, sum, planner, and nullable candidate-filter ABI symbols are exported.
- `jq -e '.interfaces.sai.path == "db.sai" and .interfaces.sal.path == "db.sal" and .skills == ["database"]' sap.json` verifies the current exported-interface policy.

- On 2026-07-07, unique-key row lookup/write helper paths stopped heap-copying index artifacts on disk. Added `mappedIndexBytesForRead` and switched typed numeric unique keys (`u64/i64/u32/i32/u8/i8/u16/i16`), pair unique keys (`u64_pair`, `u64_i64_pair`), and unique `blob_eq` row lookup paths to use mapped read regions before the existing SHA-256 and shape validation. This reduces disk hot-path allocation/copy pressure for get-row/update/upsert/delete-by-key flows while preserving the existing `mem://` owned-copy safety behavior. Verified with `zig fmt src/table.zig build.zig`, `zig test src/table.zig`, and `zig test src/db_saasm_api.zig`. This is a focused hot-path I/O optimization, not a claim of full SQLite parity; broader SQL surface, durable ACID/WAL parity, long-run operations, and disk combined append default strictness remain open.

- On 2026-07-07, the same mapped-read cleanup was extended from unique index artifacts to segment-column inputs on key write paths. `blobHandleAtRowFromMeta`, `buildColumnBuffersWithoutRow`, and `buildColumnBuffersReplacingRow` now use `mappedSegmentColumnBytes` instead of `readFileAlloc(..., 1 << 30)` when reading existing disk segment columns for unique `blob_eq` confirmation, row deletion, and row replacement. The existing file byte-count checks remain enforced through `mappedSegmentColumnBytes`, while output buffers are still built explicitly for the current rewrite-style update/delete implementation. Verified with `zig fmt src/table.zig build.zig`, `zig test src/table.zig`, and `zig test src/db_saasm_api.zig`. This removes another avoidable heap-copy layer but does not turn replacement/deletion into SQLite-style in-place/index-incremental updates.

- On 2026-07-07, selected direct/index-build blob store read paths stopped heap-copying full blob artifacts on disk. Added `validateBlobStoreBytes` and `mappedBlobStoreBytesForRead`, then switched row blob key validation, transaction `blob_eq` lookup, direct `blobValueLen` / `copyBlobValue`, direct unique `blob_eq` key lookup, and blob `eq` / token / prefix / contains index builders to use mapped blob store input while preserving entry-count, SHA-256, and block-hash validation. Unsafe pending blob bytes and `mem://` paths still use allocator-owned regions so lifetimes remain explicit. At this point `CachedBlobStore` still remained an owned cache, and blob append paths still kept owned old bytes because they construct a replacement artifact. Verified with `zig fmt src/table.zig build.zig`, `zig test src/table.zig`, and `zig test src/db_saasm_api.zig`. This reduces disk read allocation pressure for covered blob read/index paths, but it is not a full SQLite parity claim.

- On 2026-07-07, append-index blob cache reuse was moved from owned bytes to a mapped-backed cache. `CachedBlobStore` now stores a `MappedReadRegion` plus refs, `getCachedBlobStore` loads blob store inputs through `mappedBlobStoreBytesForRead`, and `releaseCachedBlobStore` releases refs plus the mapped region together. This removed the remaining full blob-store heap copy from append-only blob index maintenance while preserving ref lifetimes for `blob_eq`, token, prefix, and contains incremental index builders. Verified with `zig fmt src/table.zig build.zig`, `zig test src/table.zig`, and `zig test src/db_saasm_api.zig`. At this point blob append still used owned old bytes to construct the replacement artifact, so this was another hot-path allocation reduction rather than full SQLite parity.

- On 2026-07-07, blob append old-input reads were moved to the mapped blob store helper too. `writeTransactionPutBlobValue` and `putBlobValue` now read an existing blob store through `mappedBlobStoreBytesForRead`, pass the mapped slice into `buildBlobBytesWithValue`, and release the mapped region after the replacement artifact buffer is built. The obsolete `readBlobStoreBytes` owned-copy helper was removed so future blob store reads cannot silently regress to `readFileAlloc(..., 1 << 30)`. Verified with `zig fmt src/table.zig build.zig`, `rg -n "readBlobStoreBytes\\(" src/table.zig` returning no matches, `zig test src/table.zig`, and `zig test src/db_saasm_api.zig`. This eliminated disk heap-copying for the old input side of blob append, but at that point the artifact format still built a new replacement buffer before publish.

- On 2026-07-07, string-dictionary read and append-old-input paths were moved to mapped read regions. Added `validateDictBytes` and `mappedDictBytesForRead`, then switched transaction dictionary intern, direct `internStringDict`, direct `internStringDictMany`, `lookupStringDict`, `stringDictValueLen`, and `copyStringDictValue` to use mapped dictionary bytes on disk while preserving entry-count, SHA-256, and block-hash validation. Pending unsafe-bootstrap dictionary bytes and `mem://` roots still use allocator-owned mapped regions so lifetimes remain explicit. The obsolete `readDictBytes` owned-copy helper was removed to avoid future regressions to `readFileAlloc(..., 1 << 30)`. This removed another disk heap-copy layer for dictionary lookup/intern hot paths, but at that point dictionary append still built a new replacement artifact buffer and was not a full SQLite parity claim.

- On 2026-07-07, append-index segment-column cache and full blob text index column scans were moved to mapped read regions. `CachedColumnBytes` now stores a `MappedReadRegion` instead of owned bytes, `getCachedSegmentColumnBytes` uses `mappedSegmentColumnBytes` for disk segment inputs, and `tryAppendIndexesForAppendedRows` releases the mapped column cache with the same explicit lifetime model used by blob and dictionary mapped reads. Full `blob_token`, `blob_prefix`, and `blob_contains` index builders also now map the blob-id segment column while continuing to map blob store inputs separately. This removes another full segment-column heap copy from append-only index maintenance and blob text index construction. It does not change the current rewrite-style update/delete behavior, and it is not a full SQLite parity claim.

- On 2026-07-07, the remaining segment-column input copies in compact/full-buffer/update paths were reduced. `mergeSegmentFiles` now maps each source segment column before appending into the compacted output buffer, `buildAllColumnBuffers` maps source columns before validating and appending into rewrite buffers, and `incrementU64Range` maps the old column bytes before copying into the mutable replacement buffer. This removes avoidable input-side heap copies from compact merge, rewrite-style full-buffer rebuilds, and the direct `u64` range-update old-input path while preserving the existing merged/replacement output strategy. The remaining `readFileAlloc(..., 1 << 30)` uses are now the memory-backed `copyFile` fallback and `ingestTable` data-source read; this is still not a full SQLite parity claim.

- On 2026-07-07, the final 1GiB-cap input reads stopped heap-copying disk files. Added `mappedReadFileMaxOwned`, which mmaps bounded disk inputs and keeps allocator-owned bytes for `mem://` inputs so memory-root lifetimes stay explicit. `copyFile`'s memory-root fallback and `ingestTable` now use that helper and release through `releaseMappedRegion`. `rg -n "readFileAlloc\\([^\\n]+1 << 30\\)|readDictBytes\\(|readBlobStoreBytes\\(" src/table.zig` now returns no matches. This removes the last broad disk full-file heap-copy path, but ingest still parses one whole mapped source and is not yet a streaming SQL engine or a full SQLite parity claim.

- On 2026-07-07, `zig build test` added `check-benchmark-hot-io-guards`, backed by `tests/check_hot_io_guards.sh`. The guard rejects broad `readFileAlloc(..., 1 << 30)` heap-copy regressions, obsolete owned dictionary/blob helpers, and unmapped benchmark-relevant input paths for bounded copy/ingest inputs, append-index column cache, compact/full-buffer rebuild inputs, direct `u64` range-update old inputs, direct `u64` range-update zero-count/zero-delta no-op short circuits, direct/transaction changed-column index rebuild wiring, and direct/transaction no-op row replacement short circuits. This is hot-I/O wiring evidence only; it does not execute benchmark comparisons and is not proof of full SQLite parity.

- On 2026-07-07, CSV/JSONL `ingestTable` stopped unconditionally rebuilding all indexes after every append. After parsing a mapped input into the new segment, it now records the previous row count, publishes the segment, tries `tryAppendIndexesForSegment` with mapped segment reads, and only falls back to `rebuildIndexes` if an index kind cannot be maintained incrementally. Added a regression test that creates a unique `u64` index after CSV ingest, appends another CSV file through `ingestTable`, and verifies the appended key is visible through the read snapshot index. `check-benchmark-hot-io-guards` now also locks this indexed-ingest wiring. This removes a hidden full-index rebuild from indexed CLI ingest append paths, but ingest still parses one whole mapped source and is not proof of full SQLite parity.

- On 2026-07-07, direct `u64` range updates stopped rebuilding unrelated indexes. `updateU64ColumnAdd` now calls a column-scoped rebuild helper that keeps indexes whose key columns are untouched, while still rebuilding single-column and pair indexes that reference the updated column. Added regression coverage proving an unchanged `ID` index keeps its artifact path, the updated `POINTS` index is rebuilt, and a `(ID, POINTS)` pair index is rebuilt when its second column changes; the hot-I/O guard now locks this wiring. This reduces update-path write amplification, but affected indexes still rebuild whole artifacts and this is not proof of full SQLite parity.

- On 2026-07-07, direct `u64` range updates also gained zero-count and zero-delta no-op short circuits. `updateU64ColumnAdd` still acquires the write lock, loads writable metadata, checks lock state, validates the `u64` column, and validates range bounds first, then returns `0` before computing `next_epoch` when `update_count == 0` or `delta == 0`. Regression coverage now proves a `delta == 0` call keeps the epoch, next segment id, segment paths, and `ID` / `POINTS` / `(ID, POINTS)` index paths unchanged before the real non-zero changed-column rebuild case runs. `check-benchmark-hot-io-guards` locks that the short circuit remains before artifact publication. This removes another no-op write amplification case; real range updates still rewrite touched column files and rebuild affected whole index artifacts.

- On 2026-07-07, raw-row replacement stopped rebuilding indexes for columns whose bytes did not change. `buildColumnBuffersReplacingRow` now compares the old row slice with the replacement bytes per column, returns a `changed_columns` bitmap, and `replaceRowAtIndex` calls `rebuildIndexesForChangedColumns` so single-column and pair indexes are rebuilt only when one of their key columns changed. Added regression coverage that updates only an unindexed `NOTE` column and proves both `ID` and `POINTS` index artifact paths stay unchanged, then updates indexed `POINTS` and proves only the `POINTS` index path changes while lookups remain correct. `check-benchmark-hot-io-guards` now locks this changed-column wiring. This reduces raw-row update index write amplification, but the data files are still republished through the current rewrite-style segment model and this is not proof of full SQLite parity.

- On 2026-07-07, write-transaction row replacement now tracks the same changed-column bitmap through commit. `WriteTransaction` records changed columns plus a full-index-rebuild fallback flag; pure row replacement marks only columns whose bytes differ, `commitWriteTransaction` calls `rebuildIndexesForChangedColumns` when no full rebuild is required, and materialized pending appends/deletes still force full index rebuilds. Added regression coverage that a transaction changing only an unindexed `NOTE` column keeps `ID`, `POINTS`, and `(ID, POINTS)` index artifact paths unchanged, then a transaction changing indexed `POINTS` keeps the `ID` index while rebuilding `POINTS` and pair indexes with old/new lookup checks. `check-benchmark-hot-io-guards` now locks this transaction wiring. This reduces transaction update index write amplification, but transaction data files still rewrite and affected indexes still rebuild whole artifacts; it is not proof of full SQLite parity.

- On 2026-07-07, direct and write-transaction row replacement gained a no-op update short circuit. `changedColumnsAny` lets `replaceRowAtIndex` return the current table info before writing a new segment, epoch, or index artifact when every replacement column byte matches the existing row; `txReplaceRawRow` now returns before dirtying the transaction when no column bytes changed, while preserving earlier pending mutations if the transaction was already dirty. Regression coverage now verifies identical direct and transaction row updates keep the epoch, next segment id, segment paths, and index paths unchanged before the changed-column rebuild cases run. `check-benchmark-hot-io-guards` locks both no-op paths. This removes avoidable no-op write amplification, but changed rows still use rewrite-style data publication and affected indexes still rebuild whole artifacts.

- On 2026-07-07, direct delete-by-key stopped rebuilding every index from table columns after deleting one row. `deleteRowAtIndex` now records the previous row count, rewrites each existing index artifact by validating old index bytes, filtering the deleted row, and decrementing row ids after the deleted row, covering fixed-width single-column indexes, `u64_pair` / `u64_i64_pair`, `blob_eq`, and blob token/prefix/contains indexes. Added regression coverage that deletes a middle row and verifies the moved row is findable at its new row id through both single-column and pair indexes, and `check-benchmark-hot-io-guards` now forbids direct delete from calling full `rebuildIndexes`. Verified with `zig fmt src/table.zig build.zig`, `zig build check-benchmark-hot-io-guards`, `zig build check-docs`, `zig build check-proof-wiring`, `zig build check-benchmark-parser-guards`, `zig test src/table.zig`, `zig test src/db_saasm_api.zig`, `zig build test`, and `zig build`. This reduces direct delete index write amplification, but table data files still rewrite through the current single-segment model and transaction deletes still force full index rebuilds.

- On 2026-07-07, pure write-transaction delete gained the same index-row filtering strategy as direct delete. `WriteTransaction` now tracks original deleted row ids and whether delete-index filtering remains safe; `txDeleteRow` records original rows only while the transaction is still delete-only, and `commitWriteTransaction` rewrites existing indexes with `rewriteIndexesForDeletedRows` instead of full `rebuildIndexes` for that pure delete case. Pending append materialization, materialized inserts/appends, and row replacement disable the optimization so mixed transactions continue to use the full rebuild fallback. Added regression coverage that deletes two rows in one transaction and verifies remaining single-column and pair index lookups point at the migrated row id. `check-benchmark-hot-io-guards` now locks the state fields, original-row mapping, multi-row rewrite helper, and commit fallback wiring. Verified with `zig fmt src/table.zig build.zig`, `bash -n tests/check_hot_io_guards.sh tests/check_docs.sh tests/check_proof_wiring.sh tests/check_benchmark_parser_guards.sh`, `zig build check-benchmark-hot-io-guards`, `zig build check-docs`, `zig build check-proof-wiring`, `zig build check-benchmark-parser-guards`, `zig test src/table.zig`, and `zig test src/db_saasm_api.zig`; full `zig build test` and `zig build` remained pending before the next change. This reduces pure transaction delete index write amplification only; data files still rewrite and mixed transactions still full-rebuild indexes.

- On 2026-07-07, multi-row delete index rewrites now use a sorted deleted-row lower-bound lookup instead of scanning the whole deleted-row list for every index entry. `adjustedRowAfterDeletes` now finds the number of prior deleted rows in logarithmic time, and `recordTransactionDeletedRow` uses the same lower-bound helper to preserve sorted original-row tracking. Added blob-index regression coverage for pure write-transaction delete: after deleting two rows, `blob_eq`, `blob_token`, `blob_prefix`, and `blob_contains` indexes all filter out deleted rows and return migrated row ids without rescanning blob stores. `check-benchmark-hot-io-guards` now locks the lower-bound helper and blob delete coverage. Verified with `zig fmt src/table.zig`, `bash -n tests/check_hot_io_guards.sh`, `zig test src/table.zig --test-filter "table write transaction delete rewrites blob indexes by filtering deleted rows"`, `zig build check-benchmark-hot-io-guards`, `zig test src/table.zig`, `zig test src/db_saasm_api.zig`, `zig build check-docs`, `zig build check-proof-wiring`, `zig build check-benchmark-parser-guards`, `zig build test`, and `zig build`. This improves delete-only index-maintenance scaling and coverage, but it is not proof of full SQLite parity because data files still rewrite and mixed transactions still fall back to full index rebuilds.

- On 2026-07-07, clean write-transaction unique-key lookup now reuses committed unique index artifacts before materializing transaction column buffers. `transactionCanUseCommittedIndexLookup` limits the fast path to transactions with no dirty state, no dirty rows, no materialized buffers, no pending append buffers/rows, and no deleted rows; all typed numeric, pair, and unique `blob_eq` `txFind*KeyRow` paths call the corresponding `findUnique*` helper before falling back to the existing materialized-buffer scan. Added regression coverage that typed numeric, pair, and `blob_eq` lookups keep `tx.buffers` unmaterialized in a clean transaction. `check-benchmark-hot-io-guards` now locks the helper, all `txFind*` fast paths, and the no-materialization coverage. Verified with `zig fmt src/table.zig`, `bash -n tests/check_hot_io_guards.sh`, `zig test src/table.zig --test-filter "table clean transaction key lookup uses committed indexes without materializing buffers"`, `zig build check-benchmark-hot-io-guards`, `zig test src/table.zig`, `zig test src/db_saasm_api.zig`, `zig build check-docs`, `zig build check-proof-wiring`, `zig build check-benchmark-parser-guards`, `zig build test`, and `zig build`. This reduces clean transaction key-lookup materialization work only; non-append-only dirty transactions with pending row visibility still materialize buffers, and it is not proof of full SQLite parity.

- On 2026-07-07, append-only write-transaction unique-key lookup now avoids materializing the committed base. `transactionCanUseAppendOnlyIndexLookup` keeps the fast path limited to dirty row transactions with no materialized buffers, no deletes, pending append buffers/rows, safe delete-index state, and no full-index-rebuild fallback; each typed numeric, pair, and unique `blob_eq` `txFind*KeyRow` path validates committed indexes against `base_row_count` through `committedTransactionMeta(tx)`, returns committed hits directly, and scans only `pending_append_buffers` for committed misses. The blob path uses the transaction's current blob-store meta, so a transaction-local `writeTransactionPutBlobValue` followed by an appended row is visible without materializing base columns. Added regression coverage that committed hits, pending typed/pair/blob hits, missing keys, and a new-key `writeTransactionUpsertRawRowU64Key` keep `tx.buffers.len == 0`. Verified with `zig fmt src/table.zig`, `bash -n tests/check_hot_io_guards.sh`, `zig test src/table.zig --test-filter "table append-only transaction key lookup scans pending rows without materializing base"`, `zig build check-benchmark-hot-io-guards`, `zig test src/table.zig`, `zig test src/db_saasm_api.zig`, `zig build check-docs`, `zig build check-proof-wiring`, `zig build check-benchmark-parser-guards`, `zig build test`, and `zig build`. This reduces append-only transaction key-lookup materialization work only; committed-row replacement/delete/mixed/materialized-buffer transactions still use the conservative materialized-buffer scan path and this is not proof of full SQLite parity.

- On 2026-07-07, pending append row replacement now avoids committed-base materialization. `txReplaceRawRow` calls `txTryReplacePendingAppendRow` before `materializeTransactionBuffers`; the helper only runs when the transaction is still append-only, the target row is in `base_row_count..meta.row_count`, and pending append buffers validate, then it rewrites only `pending_append_buffers` without marking committed changed columns, forcing full index rebuild, disabling delete-index filtering, or materializing base columns. The append-only transaction regression now covers `append -> update pending key`, `append/upsert new key -> upsert same pending key`, transaction-local blob handles, no-materialization assertions, commit, and final typed/pair/blob index visibility. Verified with `zig fmt src/table.zig`, `bash -n tests/check_hot_io_guards.sh`, `zig test src/table.zig --test-filter "table append-only transaction key lookup scans pending rows without materializing base"`, `zig build check-benchmark-hot-io-guards`, `zig test src/table.zig`, `zig test src/db_saasm_api.zig`, `zig build check-docs`, `zig build check-proof-wiring`, `zig build check-benchmark-parser-guards`, `zig build test`, and `zig build`. This keeps pending-row update/upsert on the incremental append path only; committed-row replacement/delete/mixed/materialized-buffer transactions still use conservative materialization and it is not proof of full SQLite parity.

- On 2026-07-07, pending append row deletion now avoids committed-base materialization. `txDeleteRow` calls `txTryDeletePendingAppendRow` before `materializeTransactionBuffers`; the helper only runs for append-only transactions and pending row ids at or after `base_row_count`, validates the pending buffers, removes each column slice from `pending_append_buffers`, shifts later pending rows down, and leaves committed delete tracking, delete-index filtering, and full-index-rebuild fallback untouched. Deleting all pending rows now frees the pending buffers, clears row-dirty state, and becomes a no-row-change commit no-op unless transaction blob/dictionary metadata was also changed; a new `metadata_dirty` flag preserves those metadata-only commits. The append-only regression now covers deleting one pending row without materializing and row-id migration for the remaining pending row, plus a dedicated `append -> delete all pending rows` no-op test. Verified with `zig fmt src/table.zig`, `bash -n tests/check_hot_io_guards.sh`, `zig test src/table.zig --test-filter "table append-only transaction key lookup scans pending rows without materializing base"`, `zig test src/table.zig --test-filter "table append-only transaction delete all pending rows stays unmaterialized no-op"`, `zig build check-benchmark-hot-io-guards`, `zig test src/table.zig`, `zig test src/db_saasm_api.zig`, `zig build check-docs`, `zig build check-proof-wiring`, `zig build check-benchmark-parser-guards`, `zig build test`, and `zig build`. This improves append-only transaction delete hot paths only; committed-row deletes, mixed transactions, and row-count-changing materialized writes remain conservative and this is not proof of full SQLite parity.

- On 2026-07-07, write-transaction blob puts now stage blob artifact bytes until commit instead of writing the blob file at `writeTransactionPutBlobValue` time. `WriteTransaction` now owns `pending_blob_writes`, `writeTransactionPutBlobValue` reads a prior pending write for same-store appends before falling back to mapped committed blob bytes, and commit flushes pending blob artifacts before append-index maintenance so `blob_eq` incremental indexes can read the new store. Rollback/destroy frees pending blob bytes without leaving an uncommitted blob artifact. Transaction-local `blob_eq` lookup and row validation now use `transactionBlobStoreBytesForRead`, so append-only committed-index lookup, pending-append scans, materialized scans, and blob-key update/upsert validation all see the transaction blob-store view without requiring the file to exist early. Regression coverage now asserts transaction blob paths do not exist before commit, do exist after commit, and remain absent after rollback. Verified with `zig fmt src/table.zig`, `bash -n tests/check_hot_io_guards.sh`, `zig build check-benchmark-hot-io-guards`, `zig test src/table.zig --test-filter "table write transaction commits blob handles with rows atomically"`, `zig test src/table.zig --test-filter "table append-only transaction key lookup scans pending rows without materializing base"`, `zig test src/table.zig`, `zig test src/db_saasm_api.zig`, `zig build check-docs`, `zig build check-proof-wiring`, `zig build check-benchmark-parser-guards`, `zig build test`, and `zig build`. This tightens transaction atomicity and removes rollback artifact garbage for blob puts; transaction blob append still builds a replacement artifact buffer and this is not proof of full SQLite parity.

## Remaining Work

- None in the current tracked feature set. Ongoing SQLite-parity and performance work is tracked in [tasks.md](./tasks.md).
