# sa_plugin_db Progress

## Tracking Basis

- Scope: `db` plugin manifest, CLI command surface, qmod registry/execution, and local table storage helpers.
- Progress rule: update this file, including percentages, after each completed feature.
- Current status: `16 / 16` original qmod/CLI features completed (`100.0%`). Since that milestone, the plugin has moved into a development ABI phase with `db.sai` and `db.sal` exported through `sap.json` for SA-facing read handles, typed queries, row writes, dictionaries, blob stores, and ERP planner helpers. Row-list intersection, union, exclusion, candidate text/blob filtering, indexed two-predicate planners for `u64+i64`, `u64+u64`, `i64+i64`, `u64+blob_eq`, and `i64+blob_eq`, plus three-predicate planners for `u64+i64+blob_eq`, `u64+i64+bool`, `u64+i64+i64`, and semantic `u64+date+decimal_i64`, are now exposed for indexed AND/OR/not-in, text-search, and common ERP amount/date/status/business-key/flag composition. Strict fixed-width row update/upsert/delete by unique `u64`, `u8`, `i8`, `u16`, `i16`, `u32`, `i32`, and `i64` keys is available both directly and inside single-table transactions, and read handles can copy full rows by unique typed integer keys (`u64`, `i64`, `u32`, `i32`, `u8`, `i8`, `u16`, `i16`), unique `(u64, u64)` / `(u64, i64)` composite keys, and unique `blob_eq` text/blob keys for ERP form loads. Read handles now provide direct whole-column sums for both unsigned `u64` quantities and signed `i64`/scaled-decimal amounts, plus stable first-seen `u64` group counts and signed `i64` sums for full snapshots or filtered candidate row lists, including grouped `count/sum/min/max` pages and sorted pages by key/count/sum/min/max for Top N ERP reports. Unique `(u64, u64)` plus unique `(u64, i64)` composite-key upsert/update/delete is now available for ERP child rows and signed date/timestamp second-key shapes such as order lines, product/warehouse balances, customer/order-day lists, and status/due-day lists. Unique `blob_eq` text/blob business keys now support direct and transaction upsert/update/delete, with row `blob_handle` validation against the exact key bytes. Transaction-scoped dictionary intern is available through `sa_db_tx_dict_intern` / `DB_TX_DICT_INTERN`, allowing ERP status/category labels and the rows that reference them to commit or roll back together.

## Feature Progress

- Feature 1 - `sap.json` plugin manifest and permissions: 100% (`db` artifact, `database` skill, project-scoped fs permissions, no net/env/process-spawn permissions).
- Feature 2 - Runtime descriptor and CLI command handler: 100% (`database` skill section exported through the runtime descriptor).
- Feature 3 - `db init <schema.sadb-schema>` schema bootstrap: 100% (emits generated `.sai` schema interface files).
- Feature 4 - `db register <query.sa>` qmod registry: 100% (SHA-256 payload storage and declared DB grant validation).
- Feature 5 - `db inspect <table|hash>` / `db status <table>` metadata inspection: 100% (table and qmod registry metadata reporting).
- Feature 6 - `db exec <hash> [--params <file>]` supported qmod execution: 100% for current recognized scalar/read/write/read-write/atomic cursor instruction shapes.
- Feature 7 - `db ingest <table> <csv|jsonl>` table ingestion: 100% (CSV and JSONL append into column segment files).
- Feature 8 - `db snapshot <table>` and `db restore <table> <epoch>` snapshots: 100% (metadata, schema, and segment snapshot/restore).
- Feature 9 - `db verify <table>` integrity checks: 100% (schema hash, segment hashes, row counts, and segment sizes).
- Feature 10 - `db compact <table>` segment compaction: 100% (many segments merged into one active segment).
- Feature 11 - `db lock <table>` table lock: 100% (locked metadata plus readonly file modes).
- Feature 12 - `db unlock <table>` table unlock: 100% (validates current table, clears locked state, restores writable file modes, and allows ingestion again). Verified with `zig build test` on 2026-06-08.
- Feature 13 - Installed-plugin smoke verification: 100% (`tests/smoke_installed.sh` builds, dev-installs, verifies `database` skills, and runs `sa db init/ingest/verify/lock/unlock/verify`). Verified on 2026-06-08.
- Feature 14 - Command-level qmod integration examples: 100% (`docs/qmod_examples.md` documents `sa db register` plus `sa db exec`, and `tests/smoke_installed.sh` verifies register/inspect/exec against a dev-installed plugin). Verified on 2026-06-08.
- Feature 15 - Interface policy: superseded after the original qmod milestone. `sap.json` now exports plugin-shipped `db.sai` and `db.sal`; generated table `.sai` files remain per-project schema outputs.
- Feature 16 - Broader qmod execution surface: 100% (added `and`, `or`, `xor`, `shl`, and `lshr` integer binary ops to the common qmod evaluator path; covered by scalar and read-only DB qmod unit tests plus installed-plugin smoke). Verified on 2026-06-08.

## Current Verification

- `zig build test` passes in `/home/vscode/projects/sa_plugins/sa_plugin_db`.
- `zig build` passes and produces `zig-out/lib/libdb.so` for the `linux-x86_64` artifact path in `sap.json`.
- `sa build-exe benchmark_test/db_group_sum_smoke.sa -o benchmark_test/db_group_sum_smoke.out --no-incremental` plus the resulting smoke binary pass and cover `DB_GROUP_SUM_I64_BY_U64_HANDLE`, `DB_GROUP_ROWS_SUM_I64_BY_U64_HANDLE`, `DB_GROUP_SUM_I64_BY_U64_SORTED_HANDLE`, and `DB_GROUP_ROWS_SUM_I64_BY_U64_SORTED_HANDLE` for full-table grouping, paged grouping, sorted full-table Top N grouping, candidate-row grouping, sorted candidate-row grouping, and empty candidate grouping through `db.sal`.
- `sa build-exe benchmark_test/db_group_stats_smoke.sa -o benchmark_test/db_group_stats_smoke.out --no-incremental` plus the resulting smoke binary pass and cover `DB_GROUP_STATS_I64_BY_U64_HANDLE`, `DB_GROUP_ROWS_STATS_I64_BY_U64_HANDLE`, `DB_GROUP_STATS_I64_BY_U64_SORTED_HANDLE`, and `DB_GROUP_ROWS_STATS_I64_BY_U64_SORTED_HANDLE` for full-table grouped `count/sum/min/max`, sorted grouped `min/max` pages, candidate-row grouped stats, sorted candidate-row grouped stats, and empty candidate grouping through `db.sal`.
- `sa build-exe benchmark_test/db_interface_smoke.sa -o benchmark_test/db_interface_smoke.out --no-incremental` plus the resulting smoke binary pass and cover `DB_PROJECT_ROWS_HANDLE` for both populated projected pages and empty projected pages (`written_rows=0`, `required_bytes=0`) plus `DB_SUM_I64_HANDLE` signed balance aggregation through `db.sal`.
- `sa build-exe benchmark_test/db_update_row_smoke.sa -o benchmark_test/db_update_row_smoke.out --no-incremental` plus the resulting smoke binary pass and cover strict row update, transaction row update, and missing-key `SA_DB_ERR_NOT_FOUND` behavior through `db.sal`.
- `sa build-exe benchmark_test/db_i64_key_write_smoke.sa -o benchmark_test/db_i64_key_write_smoke.out --no-incremental` plus the resulting smoke binary pass and cover direct and transaction upsert/update/delete by unique signed `i64` key through `db.sal`, including negative keys and rollback invisibility.
- `sa build-exe benchmark_test/db_u32_i32_key_write_smoke.sa -o benchmark_test/db_u32_i32_key_write_smoke.out --no-incremental` plus the resulting smoke binary pass and cover direct and transaction upsert/update/delete by unique compact `u32` and `i32` keys through `db.sal`, including signed compact keys and rollback invisibility.
- `sa build-exe benchmark_test/db_u8_i8_u16_i16_key_write_smoke.sa -o benchmark_test/db_u8_i8_u16_i16_key_write_smoke.out --no-incremental` plus the resulting smoke binary pass and cover all direct and transaction upsert/update/delete macros for unique compact `u8`, `i8`, `u16`, and `i16` keys through `db.sal`, including rollback invisibility.
- `sa build-exe benchmark_test/db_typed_key_get_row_smoke.sa -o benchmark_test/db_typed_key_get_row_smoke.out --no-incremental` plus the resulting smoke binary pass and cover full fixed-width row reads by unique `i64`, `u32`, `i32`, `u8`, `i8`, `u16`, and `i16` keys through `db.sal`.
- `sa build-exe benchmark_test/db_pair_key_get_row_smoke.sa -o benchmark_test/db_pair_key_get_row_smoke.out --no-incremental` plus the resulting smoke binary pass and cover full fixed-width row reads by unique `(u64, u64)` and `(u64, i64)` keys through `db.sal`.
- `sa build-exe benchmark_test/db_blob_key_get_row_smoke.sa -o benchmark_test/db_blob_key_get_row_smoke.out --no-incremental` plus the resulting smoke binary pass and cover full fixed-width row reads by unique `blob_eq` text/blob keys through `db.sal`.
- `sa build-exe benchmark_test/db_blob_key_write_smoke.sa -o benchmark_test/db_blob_key_write_smoke.out --no-incremental` plus the resulting smoke binary pass and cover direct and transaction upsert/update/delete by unique `blob_eq` text/blob keys through `db.sal`, including exact row-handle validation and rollback invisibility.
- `sa build-exe benchmark_test/db_u64_pair_write_smoke.sa -o benchmark_test/db_u64_pair_write_smoke.out --no-incremental` plus the resulting smoke binary pass and cover direct and transaction upsert/update/delete by unique `(u64, u64)` key through `db.sal`.
- `sa build-exe benchmark_test/db_u64_i64_pair_write_smoke.sa -o benchmark_test/db_u64_i64_pair_write_smoke.out --no-incremental` plus the resulting smoke binary pass and cover direct and transaction upsert/update/delete by unique `(u64, i64)` key through `db.sal`, including negative signed second keys and rollback invisibility.
- `sa build-exe benchmark_test/db_tx_dict_smoke.sa -o benchmark_test/db_tx_dict_smoke.out --no-incremental` plus the resulting smoke binary pass and cover transaction dictionary intern, commit visibility, row references, and rollback invisibility through `db.sal`.
- `sa build-exe benchmark_test/db_candidate_filter_smoke.sa -o benchmark_test/db_candidate_filter_smoke.out --no-incremental` plus the resulting smoke binary pass and cover candidate row filters, intersection, union, exclusion, stats, and sorting through `db.sal`.
- `sa build-exe benchmark_test/db_planner_smoke.sa -o benchmark_test/db_planner_smoke.out --no-incremental` plus the resulting smoke binary pass and cover `DB_PLAN_U64_I64_RANGES_HANDLE`, `DB_PLAN_U64_U64_RANGES_HANDLE`, `DB_PLAN_I64_I64_RANGES_HANDLE`, `SaDbPlanInfo`, smaller-side choice, and pagination through `db.sal`.
- `sa build-exe benchmark_test/db_blob_planner_smoke.sa -o benchmark_test/db_blob_planner_smoke.out --no-incremental` plus the resulting smoke binary pass and cover `DB_PLAN_U64_BLOB_EQ_HANDLE`, `DB_PLAN_I64_BLOB_EQ_HANDLE`, `DB_PLAN_U64_I64_BLOB_EQ_HANDLE`, `SaDbPlanInfo`, `SaDbPlan3Info`, smaller-side choice, range-first/blob-first plans, three-predicate planning, and pagination through `db.sal`.
- `sa build-exe benchmark_test/db_bool_planner_smoke.sa -o benchmark_test/db_bool_planner_smoke.out --no-incremental` plus the resulting smoke binary pass and cover `DB_PLAN_U64_I64_BOOL_HANDLE`, `SaDbPlan3Info`, i64-first/bool-first/u64-first plans, three-predicate planning, and pagination through `db.sal`.
- `sa build-exe benchmark_test/db_i64_i64_planner_smoke.sa -o benchmark_test/db_i64_i64_planner_smoke.out --no-incremental` plus the resulting smoke binary pass and cover `DB_PLAN_U64_I64_I64_RANGES_HANDLE`, `SaDbPlan3Info`, amount-first/date-first/u64-first plans, three-predicate planning, and pagination through `db.sal`.
- `sa build-exe benchmark_test/db_date_decimal_planner_smoke.sa -o benchmark_test/db_date_decimal_planner_smoke.out --no-incremental` plus the resulting smoke binary pass and cover `DB_PLAN_U64_DATE_DECIMAL_I64_HANDLE`, `SaDbPlan3Info`, semantic date/decimal boundary conversion, amount-first/date-first plans, and invalid-date rejection through `db.sal`.
- `sa build-exe` plus execution pass for `db_blob_filter_smoke.sa`, `db_blob_token_smoke.sa`, and `db_blob_prefix_smoke.sa`, covering candidate row text/blob exact, contains, token, and prefix filters through `db.sal`.
- `bash tests/smoke_installed.sh` passes and verifies the dev-installed plugin command path, including qmod register/inspect/exec and a bitwise DB filter qmod.
- `nm -D zig-out/lib/libdb.so | rg 'sa_db_group_(rows_)?sum_i64_by_u64(_sorted)?_handle$|sa_db_sum_(u64|i64)_handle$|sa_db_plan_(u64_i64|u64_u64|i64_i64)_ranges_handle$|sa_db_plan_(u64|i64)_blob_eq_handle$|sa_db_plan_u64_i64_(blob_eq|bool|i64_ranges)_handle$|sa_db_plan_u64_date_decimal_i64_handle$'` verifies the group, sum, and planner ABI symbols are exported.
- `jq -e '.interfaces.sai.path == "db.sai" and .interfaces.sal.path == "db.sal" and .skills == ["database"]' sap.json` verifies the current exported-interface policy.

## Remaining Work

- None in the current tracked feature set.
